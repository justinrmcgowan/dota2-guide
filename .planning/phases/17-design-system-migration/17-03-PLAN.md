---
phase: 17-design-system-migration
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - prismlab/frontend/src/components/timeline/ItemCard.tsx
  - prismlab/frontend/src/components/timeline/PhaseCard.tsx
  - prismlab/frontend/src/components/timeline/ItemTimeline.tsx
  - prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx
  - prismlab/frontend/src/components/timeline/NeutralItemSection.tsx
  - prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx
  - prismlab/frontend/src/components/game/GameStatePanel.tsx
  - prismlab/frontend/src/components/game/LiveStatsBar.tsx
  - prismlab/frontend/src/components/game/DamageProfileInput.tsx
  - prismlab/frontend/src/components/game/EnemyItemTracker.tsx
  - prismlab/frontend/src/components/game/LaneResultSelector.tsx
  - prismlab/frontend/src/components/game/ReEvaluateButton.tsx
autonomous: true
requirements: [DESIGN-03, DESIGN-05]
must_haves:
  truths:
    - "Phase cards render with surface-container-low background and no visible borders"
    - "Item cards with core or luxury priority show a 2px gold (#FFE16D) left-accent strip"
    - "Purchased-item checkmarks retain rounded-full (per D-04 exemption)"
    - "The reasoning panel uses font-display for item name labels"
    - "LiveStatsBar uses tonal background shifts instead of borders for stat separators"
    - "NeutralItemSection rank #1 badge uses secondary instead of cyan-accent"
  artifacts:
    - path: "prismlab/frontend/src/components/timeline/ItemCard.tsx"
      provides: "Monolith card with gold accent strip"
      contains: "secondary-fixed"
    - path: "prismlab/frontend/src/components/timeline/PhaseCard.tsx"
      provides: "Phase card with surface-container-low background"
      contains: "bg-surface-container-low"
  key_links:
    - from: "prismlab/frontend/src/components/timeline/ItemCard.tsx"
      to: "DESIGN.md Monolith card pattern"
      via: "gold left-accent strip on core/luxury items"
      pattern: "border-l-secondary-fixed"
    - from: "prismlab/frontend/src/components/timeline/PhaseCard.tsx"
      to: "prismlab/frontend/src/components/timeline/ItemCard.tsx"
      via: "renders ItemCard children"
      pattern: "ItemCard"
---

<objective>
Migrate all timeline and game-state components to the DESIGN.md design system. This covers the main content area: phase cards, item cards (with the Monolith pattern and gold accents per D-10), neutral items, live stats, and mid-game adaptation inputs.

Purpose: The item timeline is the most-viewed surface in the app -- it must look editorial.
Output: 12 files updated with new tokens, Monolith card pattern, and gold accent strips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/17-design-system-migration/17-01-SUMMARY.md

@prismlab/frontend/src/components/timeline/ItemCard.tsx
@prismlab/frontend/src/components/timeline/PhaseCard.tsx
@prismlab/frontend/src/components/timeline/NeutralItemSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate timeline components (ItemCard, PhaseCard, ItemTimeline, DecisionTreeCard, NeutralItemSection, LoadingSkeleton)</name>
  <files>
    prismlab/frontend/src/components/timeline/ItemCard.tsx,
    prismlab/frontend/src/components/timeline/PhaseCard.tsx,
    prismlab/frontend/src/components/timeline/ItemTimeline.tsx,
    prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx,
    prismlab/frontend/src/components/timeline/NeutralItemSection.tsx,
    prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx
  </files>
  <read_first>
    prismlab/frontend/src/components/timeline/ItemCard.tsx,
    prismlab/frontend/src/components/timeline/PhaseCard.tsx,
    prismlab/frontend/src/components/timeline/ItemTimeline.tsx,
    prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx,
    prismlab/frontend/src/components/timeline/NeutralItemSection.tsx,
    prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx,
    DESIGN.md
  </read_first>
  <action>
Apply the Monolith card pattern (D-10) and gold accent strips (D-05/DESIGN-05).

**ItemCard.tsx** (per D-10: Monolith card pattern):
- REMOVE `rounded-md` from the button
- `border-l-2` stays -- this is the accent strip
- PRIORITY_BORDER map: Replace completely:
  - `core`: `"border-l-secondary-fixed"` (gold #FFE16D per D-10)
  - `luxury`: `"border-l-secondary-fixed"` (gold #FFE16D, same as core per D-10: "Hero or Legendary item")
  - `situational`: `"border-l-outline-variant"` (ghost border #5A403E, subdued)
- Selected state: `ring-1 ring-cyan-accent bg-bg-elevated/50` -> `ring-1 ring-secondary-fixed bg-surface-container-high/50`
- Hover: `hover:bg-bg-elevated/30` -> `hover:bg-surface-container-high/30`
- Item image: `rounded` -> keep per D-05 (item images use roundedness for visual softness)
- PURCHASED CHECKMARK: `rounded-full` MUST STAY per D-04 (explicit exemption). Keep `bg-radiant` on the checkmark circle.
- Gold cost text: `text-amber-400` -> `text-secondary` (#FFDB3C gold)
- Item name fallback: `text-amber-400` -> `text-secondary`

**PhaseCard.tsx** (per D-10: Monolith card with no dividers):
- Container: `bg-bg-secondary rounded-lg p-4 border border-bg-elevated` -> `bg-surface-container-low p-[1.75rem]` (1.75rem = Spacing Scale 8 per DESIGN.md). REMOVE `rounded-lg`, REMOVE `border border-bg-elevated` (No-Line Rule).
- PHASE_COLORS map: Replace:
  - `starting`: `"text-on-surface-variant"` (subdued phase)
  - `laning`: `"text-primary"` (#FFB4AC crimson, active phase)
  - `core`: `"text-secondary"` (#FFDB3C gold, peak phase)
  - `late_game`: `"text-tertiary"` (#8DA4B8 slate)
  - `situational`: `"text-secondary"` (#FFDB3C gold)
- Phase header `h3`: add `font-display` (Newsreader for phase label headlines)
- Timing/gold_budget spans: `text-gray-500` -> `text-on-surface-variant`
- Reasoning panel: `bg-bg-elevated/50 rounded-lg p-4 mt-3 border-l-2 border-cyan-accent` -> `bg-surface-container-high/50 p-[1.75rem] mt-[1.75rem] border-l-2 border-secondary-fixed` REMOVE `rounded-lg`
- Reasoning label: `text-cyan-accent` -> `text-secondary` (gold for label-md highlights per D-13)
- Reasoning body: `text-gray-300` -> `text-on-surface-variant`

**ItemTimeline.tsx**:
- Strategy label: `text-cyan-accent` -> `text-secondary font-display` (gold headline)
- Strategy body: `text-gray-400` -> `text-on-surface-variant`

**DecisionTreeCard.tsx**:
- Container: `bg-bg-elevated/30 rounded-lg p-3 border border-amber-500/20` -> `bg-surface-container-high/30 p-[1.75rem]` REMOVE `rounded-lg` and `border border-amber-500/20`
- Title: `text-amber-400` -> `text-secondary font-display`
- `divide-y divide-bg-elevated` -> REMOVE (Monolith: no dividers). Use `gap-[1.75rem]` spacing instead. Change from `divide-y` parent to `flex flex-col gap-[1.75rem]` and remove `py-2 first:pt-0 last:pb-0` from children.
- Condition text: `text-gray-400` -> `text-on-surface-variant`
- Arrow icon: `text-gray-600` -> `text-outline-variant`
- Item name: `text-gray-300` -> `text-on-surface`
- Item image: keep `rounded` per D-05

**NeutralItemSection.tsx**:
- Container: `bg-bg-secondary rounded-lg p-4 border border-bg-elevated` -> `bg-surface-container-low p-[1.75rem]` REMOVE `rounded-lg` and `border`
- Section header: `text-cyan-accent` -> `text-secondary font-display`
- Tier sub-containers: `bg-bg-elevated/50 rounded-lg p-3` -> `bg-surface-container-high/50 p-3` REMOVE `rounded-lg`
- Active tier ring: `ring-1 ring-cyan-accent` -> `ring-1 ring-secondary-fixed`
- Tier label: `text-gray-400` -> `text-on-surface-variant`
- RANK_COLORS map:
  - 1: `"bg-cyan-accent text-bg-primary"` -> `"bg-secondary text-surface"` (gold badge)
  - 2: `"bg-gray-600 text-gray-200"` -> `"bg-surface-container-high text-on-surface"`
  - 3: `"bg-gray-700 text-gray-300"` -> `"bg-surface-container text-on-surface-variant"`
- Rank badge: keep `rounded-full` per D-05 (functional: rank indicator circle)
- Item name: `text-gray-300` -> `text-on-surface`
- Reasoning: `text-gray-400` -> `text-on-surface-variant`
- Countdown: `text-text-muted` -> `text-on-surface-variant`
- Item image: keep `rounded` per D-05

**LoadingSkeleton.tsx**:
- Phase card: `bg-bg-secondary rounded-lg p-4 border border-bg-elevated` -> `bg-surface-container-low p-[1.75rem]` REMOVE `rounded-lg` and `border`
- Item blocks: `bg-bg-elevated rounded` -> `bg-surface-container-high` REMOVE `rounded`
- Label pulse: `text-gray-600` -> `text-on-surface-variant/30`
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "cyan-accent" src/components/timeline/ItemCard.tsx src/components/timeline/PhaseCard.tsx src/components/timeline/ItemTimeline.tsx src/components/timeline/DecisionTreeCard.tsx src/components/timeline/NeutralItemSection.tsx src/components/timeline/LoadingSkeleton.tsx 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero instances of `cyan-accent` across all 6 timeline component files
    - ItemCard.tsx: PRIORITY_BORDER core and luxury use `border-l-secondary-fixed`
    - ItemCard.tsx: purchased checkmark retains `rounded-full` (D-04 exemption)
    - PhaseCard.tsx: container uses `bg-surface-container-low`, no `rounded-lg`, no `border border-bg-elevated`
    - PhaseCard.tsx: phase labels use `font-display`
    - NeutralItemSection.tsx: rank 1 badge uses `bg-secondary text-surface`
    - DecisionTreeCard.tsx: no `divide-y`, uses gap spacing instead
    - npm run build succeeds
  </acceptance_criteria>
  <done>All 6 timeline components migrated. Monolith card pattern applied: surface-container-low fill, no dividers, 1.75rem spacing. Gold accent strips on core/luxury items. Purchased checkmarks exempt from 0px corners.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate game-state components (LiveStatsBar, GameStatePanel, DamageProfileInput, EnemyItemTracker, LaneResultSelector, ReEvaluateButton)</name>
  <files>
    prismlab/frontend/src/components/game/LiveStatsBar.tsx,
    prismlab/frontend/src/components/game/GameStatePanel.tsx,
    prismlab/frontend/src/components/game/DamageProfileInput.tsx,
    prismlab/frontend/src/components/game/EnemyItemTracker.tsx,
    prismlab/frontend/src/components/game/LaneResultSelector.tsx,
    prismlab/frontend/src/components/game/ReEvaluateButton.tsx
  </files>
  <read_first>
    prismlab/frontend/src/components/game/LiveStatsBar.tsx,
    prismlab/frontend/src/components/game/GameStatePanel.tsx,
    prismlab/frontend/src/components/game/DamageProfileInput.tsx,
    prismlab/frontend/src/components/game/EnemyItemTracker.tsx,
    prismlab/frontend/src/components/game/LaneResultSelector.tsx,
    prismlab/frontend/src/components/game/ReEvaluateButton.tsx,
    DESIGN.md
  </read_first>
  <action>
Migrate all game-state panel components to DESIGN.md tokens. These are in the sidebar, so they sit on the surface-container-lowest (#0E0E0E) background.

**LiveStatsBar.tsx** (Tactical HUD component per DESIGN.md Section 5):
- Container: `rounded-lg p-3 border border-bg-elevated` -> `p-3 bg-tertiary-container/20` REMOVE `rounded-lg` and `border border-bg-elevated`. Use tertiary-container (#4E5E6D) at low opacity for Tactical HUD background per DESIGN.md "Tactical HUD" section.
- Gold pulse: `bg-radiant/20` -> KEEP (game color, meaningful)
- Death pulse: `bg-dire/20` -> KEEP (game color, meaningful)
- Default bg: `bg-bg-elevated/50` -> `bg-tertiary-container/20`
- Gold value: `text-amber-400` -> `text-secondary` (#FFDB3C)
- Gold suffix: `text-amber-400/60` -> `text-secondary/60`
- Stat separators: `border-l border-bg-elevated` -> REMOVE border-l per No-Line Rule. Add `pl-3` (keep spacing) but remove the border. Separation comes from the gold vs muted text contrast.
- `text-text-muted` -> `text-on-surface-variant`
- KDA: keep `text-radiant` and `text-dire` (game colors)
- KDA separator: `text-text-muted/40` -> `text-on-surface-variant/40`

**GameStatePanel.tsx**:
- `border-t border-bg-elevated` -> REMOVE (No-Line Rule). Add `mt-6` for extra spacing instead.
- Section heading `text-gray-400` -> `text-on-surface-variant`
- Sub-headings `text-gray-500` -> `text-on-surface-variant/70`
- "Parse Screenshot" button: `text-cyan-accent bg-cyan-accent/10 border border-cyan-accent/30 rounded-md` -> `text-primary bg-primary-container/15 border border-outline-variant/15` REMOVE `rounded-md`
- `hover:bg-cyan-accent/20` -> `hover:bg-primary-container/25`
- Chevron icon: `text-gray-400` -> `text-on-surface-variant`

**DamageProfileInput.tsx**:
- Preset buttons: apply same toggle pattern as draft components:
  - Active: `bg-cyan-accent/20 text-cyan-accent border-cyan-accent` -> `bg-primary-container/20 text-primary border-primary/40`
  - Inactive: `bg-bg-elevated text-gray-400 border-bg-elevated` -> `bg-surface-container-high text-on-surface-variant border-outline-variant/15`
  - Remove `rounded-md`
- Slider label: `text-gray-400` -> `text-on-surface-variant`
- Slider track: `bg-bg-elevated rounded-lg` -> `bg-surface-container-high` REMOVE `rounded-lg`
- Slider value: `text-gray-300` -> `text-on-surface`
- Hint text: `text-gray-600` -> `text-on-surface-variant/50`
- SLIDER_CONFIG accent colors: update magical `"#00d4ff"` (cyan) -> `"#FFB4AC"` (primary crimson). Physical `"#9ca3af"` and Pure `"#a855f7"` can stay or update to `"#E2BEBA"` (on-surface-variant) and `"#8DA4B8"` (tertiary) respectively.

**EnemyItemTracker.tsx**:
- Item button: `rounded-md` -> REMOVE
- Spotted state: `ring-2 ring-dire` -> KEEP (game color, meaningful: enemy item = danger)
- Item image: `rounded-sm` -> REMOVE (0px corners)
- Label: `text-gray-400` -> `text-on-surface-variant`

**LaneResultSelector.tsx**:
- The LANE_RESULT_OPTIONS are in constants.ts (handled in Plan 02). This component just reads those options.
- Inactive state: `bg-bg-elevated text-gray-400 border-bg-elevated` -> `bg-surface-container-high text-on-surface-variant border-outline-variant/15`
- Remove `rounded-md`
- Auto-detected hint: `text-gray-500` -> `text-on-surface-variant/50`

**ReEvaluateButton.tsx** (same Blade pattern as GetBuildButton):
- Remove `rounded-lg`
- Enabled: `bg-cyan-accent text-bg-primary` -> `bg-primary-container text-on-surface font-display`
- Hover: remove `hover:brightness-110 shadow-[0_0_15px_rgba(0,212,255,0.3)]` -> add `hover:outline hover:outline-1 hover:outline-[#AA8986] shadow-glow-active`
- Disabled: `bg-bg-elevated text-gray-500` -> `bg-surface-container-high text-on-surface-variant/40`
- Cooldown text: `text-gray-500` -> `text-on-surface-variant/50`
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "cyan-accent" src/components/game/LiveStatsBar.tsx src/components/game/GameStatePanel.tsx src/components/game/DamageProfileInput.tsx src/components/game/EnemyItemTracker.tsx src/components/game/LaneResultSelector.tsx src/components/game/ReEvaluateButton.tsx 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero instances of `cyan-accent` across all 6 game component files
    - LiveStatsBar.tsx: no `border-l` separators, no `rounded-lg`, uses `text-secondary` for gold
    - GameStatePanel.tsx: no `border-t`, Parse Screenshot button uses `text-primary` and `bg-primary-container/15`
    - DamageProfileInput.tsx: preset buttons use primary-container active pattern, no `rounded-md`
    - EnemyItemTracker.tsx: spotted items still use `ring-dire` (game color preserved)
    - ReEvaluateButton.tsx: uses `bg-primary-container` enabled state (Blade pattern)
    - npm run build succeeds
  </acceptance_criteria>
  <done>All 6 game-state components migrated. LiveStatsBar uses Tactical HUD (tertiary-container) background. Buttons follow Blade pattern. No-Line Rule applied throughout. Game colors (radiant/dire) preserved for meaningful indicators.</done>
</task>

</tasks>

<verification>
1. `cd prismlab/frontend && npm run build` succeeds
2. `grep -r "cyan-accent" src/components/timeline/ src/components/game/` returns zero matches
3. `grep "border-l-secondary-fixed" src/components/timeline/ItemCard.tsx` returns match (gold accent strip)
4. `grep "rounded-full" src/components/timeline/ItemCard.tsx` returns match (checkmark exemption)
5. `grep "bg-surface-container-low" src/components/timeline/PhaseCard.tsx` returns match
</verification>

<success_criteria>
- All timeline components use Monolith card pattern (surface-container-low, no dividers, 1.75rem spacing)
- Gold accent strips on core/luxury item cards
- All game-state components use DESIGN.md tokens
- Purchased-item checkmarks retain rounded-full
- No cyan-accent references remain in timeline or game components
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-migration/17-03-SUMMARY.md`
</output>
