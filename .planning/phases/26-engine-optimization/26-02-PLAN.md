---
phase: 26-engine-optimization
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - prismlab/frontend/src/types/recommendation.ts
  - prismlab/frontend/src/components/settings/SettingsPanel.tsx
  - prismlab/frontend/src/api/client.ts
autonomous: true
requirements:
  - ENG-02  # Default Auto mode in UI (D-02)
  - ENG-05  # Budget display in Settings (D-10, D-11)
  - ENG-06  # Mode selector in Settings panel

must_haves:
  truths:
    - "User can select Fast/Auto/Deep mode from the Settings panel"
    - "Selected mode persists in localStorage and is sent with every recommend request"
    - "Current month API usage and budget are displayed in Settings"
    - "Budget warning shows amber when >80% used"
  artifacts:
    - path: "prismlab/frontend/src/components/settings/SettingsPanel.tsx"
      provides: "Mode selector radio buttons + budget display section"
      contains: "recommendation_mode"
    - path: "prismlab/frontend/src/types/recommendation.ts"
      provides: "Updated RecommendRequest with mode field, RecommendResponse with engine_mode + budget"
      contains: "engine_mode"
    - path: "prismlab/frontend/src/api/client.ts"
      provides: "getEngineBudget API method"
      contains: "engine"
  key_links:
    - from: "prismlab/frontend/src/components/settings/SettingsPanel.tsx"
      to: "prismlab/frontend/src/api/client.ts"
      via: "api.getEngineBudget() for budget display"
      pattern: "getEngineBudget"
    - from: "prismlab/frontend/src/api/client.ts"
      to: "/api/settings/budget"
      via: "fetchJson for budget endpoint"
      pattern: "settings/budget"
---

<objective>
Add mode selector and API budget display to the frontend Settings panel, wire mode to recommendation requests.

Purpose: Users need to control recommendation speed/quality tradeoff and monitor API costs directly from the UI (per D-02, D-10, D-11).
Output: Settings panel has 3-mode radio selector + budget progress bar. Mode is sent with every recommend request.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-engine-optimization/26-CONTEXT.md
@.planning/phases/26-engine-optimization/26-01-SUMMARY.md

@prismlab/frontend/src/components/settings/SettingsPanel.tsx
@prismlab/frontend/src/api/client.ts
@prismlab/frontend/src/types/recommendation.ts

<interfaces>
<!-- Backend API contracts from Plan 01 -->

GET /api/settings/budget returns:
```json
{
  "month": "2026-03",
  "cost": 12.50,
  "requests": 650,
  "budget": 30.0,
  "exceeded": false,
  "warning": false
}
```

RecommendRequest now accepts:
```json
{
  "mode": "fast" | "auto" | "deep" | null,
  ... existing fields
}
```

RecommendResponse now includes:
```json
{
  "engine_mode": "fast" | "auto" | "deep",
  "budget_used": 12.50,
  "budget_limit": 30.0,
  ... existing fields
}
```

From SettingsPanel.tsx:
```tsx
interface SettingsPanelProps {
  open: boolean;
  onClose: () => void;
}
// Currently has: GSI config section, Steam ID section
// Design tokens: bg-surface-container-highest, text-on-surface, text-secondary, border-outline-variant/15
// Input style: bg-surface-container-lowest border-b pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types + API client for mode and budget</name>
  <files>
    prismlab/frontend/src/types/recommendation.ts,
    prismlab/frontend/src/api/client.ts
  </files>
  <action>
    **1. Update RecommendRequest in types/recommendation.ts**:
    - Add `mode?: "fast" | "auto" | "deep" | null;` — optional field, null means use server default

    **2. Update RecommendResponse in types/recommendation.ts**:
    - Add `engine_mode?: "fast" | "auto" | "deep" | null;` — which mode was actually used
    - Add `budget_used?: number | null;` — current month spend in dollars
    - Add `budget_limit?: number | null;` — monthly cap in dollars

    **3. Add EngineBudget type in types/recommendation.ts**:
    ```typescript
    export interface EngineBudget {
      month: string;
      cost: number;
      requests: number;
      budget: number;
      exceeded: boolean;
      warning: boolean;
    }
    ```

    **4. Add API methods in api/client.ts**:
    - `getEngineBudget: () => fetchJson<EngineBudget>("/settings/budget")` — fetch current month budget status
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <done>RecommendRequest has mode field. RecommendResponse has engine_mode + budget fields. EngineBudget type exists. API client has getEngineBudget method. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Mode selector + budget display in SettingsPanel</name>
  <files>
    prismlab/frontend/src/components/settings/SettingsPanel.tsx
  </files>
  <action>
    **1. Add mode state with localStorage persistence** (per D-02):
    - `const [mode, setMode] = useState(() => localStorage.getItem("prismlab_engine_mode") ?? "auto")`
    - On change: `localStorage.setItem("prismlab_engine_mode", value)` and `setMode(value)`
    - "auto" is the default (D-02)

    **2. Add budget state with API fetch**:
    - `const [budget, setBudget] = useState<EngineBudget | null>(null)`
    - `useEffect` on panel open: call `api.getEngineBudget().then(setBudget).catch(() => {})` — silent fail, not critical
    - Only fetch when `open` is true

    **3. Add "Recommendation Engine" section** after the Steam ID section:
    - Section header: `<h3>` with "Recommendation Engine" — same uppercase tracking-wider style as other section headers

    **4. Mode selector** — 3 radio-style buttons in a column (per D-01):
    - **Fast** — label: "Fast", description: "Rules only. Instant. No AI reasoning."
    - **Auto** — label: "Auto" + "(default)" badge, description: "Local AI + Claude fallback. Best balance."
    - **Deep** — label: "Deep", description: "Always Claude API. Full reasoning. Highest cost."
    - Style: Each option is a clickable row with radio indicator (filled circle when selected). Use surface-container-lowest background, border-b dividers. Selected option has primary-colored left border or radio fill.
    - Follow existing design tokens: bg-surface-container-lowest, border-outline-variant/15, text-on-surface for labels, text-on-surface-variant for descriptions.

    **5. Budget display section** (per D-10, D-11):
    - Only show if `budget !== null` (API returned data)
    - Header: "API Budget (Monthly)"
    - Progress bar: `budget.cost / budget.budget * 100`% width, max 100%
      - Color: primary (normal), amber/warn (>80%), dire/red (exceeded)
    - Text below bar: `"$X.XX / $Y.YY used (Z requests)"` — formatted to 2 decimal places
    - If `budget.warning` (>80%): show amber text "Approaching budget limit"
    - If `budget.exceeded`: show dire text "Budget exceeded -- Auto mode uses local AI only"

    **6. Wire mode to recommend requests**:
    - The mode value from localStorage needs to be read wherever `api.recommend()` is called. The cleanest approach: read `localStorage.getItem("prismlab_engine_mode")` at call time and spread into the request. Find all callsites of `api.recommend()` (likely in a hook like useRecommendation.ts or useGameIntelligence.ts) and add `mode: localStorage.getItem("prismlab_engine_mode") || undefined` to the request object.
    - Alternatively, update the `api.recommend()` wrapper to auto-inject mode from localStorage. Prefer the wrapper approach for DRY.
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit --strict 2>&1 | head -20</automated>
  </verify>
  <done>Settings panel shows 3-mode radio selector defaulting to "auto". Mode persists in localStorage. Budget progress bar shows current month spend vs limit. Budget warning/exceeded states have distinct visual treatment. Mode is injected into every recommend() request.</done>
</task>

</tasks>

<verification>
1. `cd prismlab/frontend && npx tsc --noEmit --strict` — zero type errors
2. `cd prismlab/frontend && npx vite build` — production build succeeds
3. Manual: Open Settings panel, verify mode selector appears with 3 options, Auto selected by default
4. Manual: Verify mode persists after closing/reopening Settings panel
</verification>

<success_criteria>
- Mode selector with Fast/Auto/Deep radio buttons visible in Settings panel
- Auto is default (D-02) and persists in localStorage
- Budget progress bar displays with correct percentages
- Warning (amber) at 80% and exceeded (red) states are visually distinct
- Mode is sent as part of every recommendation request to the backend
- TypeScript compiles cleanly with new types
</success_criteria>

<output>
After completion, create `.planning/phases/26-engine-optimization/26-02-SUMMARY.md`
</output>
