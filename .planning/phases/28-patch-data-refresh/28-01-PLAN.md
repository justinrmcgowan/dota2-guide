---
phase: 28-patch-data-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/backend/data/seed.py
  - prismlab/backend/tests/conftest.py
  - prismlab/backend/tests/test_patch_741.py
autonomous: true
requirements: [PATCH-01, PATCH-02]
must_haves:
  truths:
    - "After re-seed, all 9 new 7.41 items exist in the database with correct costs"
    - "After re-seed, Cornucopia no longer appears as a purchasable item"
    - "After re-seed, changed item costs reflect 7.41 values (Shiva's 4500g, Blade Mail 2300g)"
    - "seed.py uses upsert for HeroAbilityData instead of insert (no IntegrityError on re-seed)"
  artifacts:
    - path: "prismlab/backend/data/seed.py"
      provides: "Fixed seed with upsert for ability data"
      contains: "merge"
    - path: "prismlab/backend/tests/test_patch_741.py"
      provides: "Automated 7.41 data correctness tests"
      min_lines: 60
    - path: "prismlab/backend/tests/conftest.py"
      provides: "Updated test fixtures with 7.41 item costs"
      contains: "cost=4500"
  key_links:
    - from: "prismlab/backend/tests/test_patch_741.py"
      to: "prismlab/backend/data/cache.py"
      via: "data_cache item lookups"
      pattern: "data_cache"
---

<objective>
Fix seed.py upsert bug, update test fixtures for 7.41 costs, and create automated pytest
tests that validate 7.41 data correctness (new items exist, removed items gone, costs correct).

Purpose: Ensure the database reflects patch 7.41 after a re-seed, and that data correctness
is automatically verified on every future patch via repeatable tests.
Output: Fixed seed.py, updated conftest.py, new test_patch_741.py
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-patch-data-refresh/28-CONTEXT.md
@prismlab/backend/data/seed.py
@prismlab/backend/data/refresh.py
@prismlab/backend/data/cache.py
@prismlab/backend/tests/conftest.py
@prismlab/backend/tests/test_system_prompt.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From prismlab/backend/data/cache.py:
```python
class DataCache:
    def item_name_to_id(self, internal_name: str) -> int | None
    def get_item(self, item_id: int) -> ItemCached | None
    def get_all_items(self) -> list[ItemCached]

@dataclass(frozen=True)
class ItemCached:
    id: int
    name: str
    internal_name: str
    cost: int | None
    components: tuple | None
    is_recipe: bool
    is_neutral: bool
    tier: int | None
```

From prismlab/backend/data/models.py:
```python
class Item(Base):
    id, name, internal_name, cost, components, is_recipe, is_neutral, tier, ...

class HeroAbilityData(Base):
    hero_id (PK), abilities_json (JSON), updated_at
```

From prismlab/backend/data/seed.py (current bug):
```python
# Line 151: uses session.add() -- causes IntegrityError on re-seed
session.add(HeroAbilityData(hero_id=hero_id, abilities_json=hero_ability_dict))
# Should use session.merge() like refresh.py does
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix seed.py upsert bug and update test fixtures for 7.41</name>
  <files>prismlab/backend/data/seed.py, prismlab/backend/tests/conftest.py</files>
  <action>
1. In seed.py, fix the HeroAbilityData insertion (line ~151) to use upsert pattern instead of
   session.add(). Change to session.merge() to match refresh.py's pattern. This fixes the
   IntegrityError on re-seed mentioned in CONTEXT.md specifics section. Per D-01/D-02.

2. In tests/conftest.py, update the test item fixtures to reflect 7.41 costs:
   - Shiva's Guard (id=119): cost=4750 -> cost=4500 (7.41 price reduction)
   - Add new test items needed for 7.41 validation:
     - Item(id=600, name="Splintmail", internal_name="splintmail", cost=950) -- new 7.41 component
     - Item(id=601, name="Blade Mail", internal_name="blade_mail", cost=2300, components=["splintmail", "broadsword", "robe"]) -- updated recipe/cost
   These items are test fixtures only. Real data comes from OpenDota re-seed.

Note: The actual data refresh (re-seeding from OpenDota) happens at deploy time via the
existing refresh pipeline. These test fixtures let us validate expectations offline.
  </action>
  <verify>
    <automated>cd prismlab/backend && python -m pytest tests/test_rules.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>seed.py uses merge() for HeroAbilityData; conftest item costs match 7.41 values; existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create automated 7.41 data correctness test suite</name>
  <files>prismlab/backend/tests/test_patch_741.py</files>
  <behavior>
    - Test: New 7.41 items (Wizard Hat, Shawl, Splintmail, Chasm Stone, Consecrated Wraps, Essence Distiller, Crella's Crozier, Hydra's Breath) exist in DB by internal_name
    - Test: Cornucopia is NOT present in the item catalog
    - Test: Shiva's Guard cost == 4500
    - Test: Blade Mail cost == 2300
    - Test: Rules engine does not reference "cornucopia" in any item lookup
    - Test: System prompt token budget still under 5000 after any 7.41 additions
    - Test: EXCLUDED_ITEMS set does not contain any valid purchasable 7.41 items
  </behavior>
  <action>
Create prismlab/backend/tests/test_patch_741.py with the following test classes. Per D-08/D-09.

**Class TestPatch741Items:**
Tests run against the live seeded database (using the test_db_session fixture from conftest).
After re-seed, these verify data correctness:

- test_new_items_exist: For each of the 8 new 7.41 items, assert data_cache.item_name_to_id(internal_name)
  is not None. Internal names to check: "wizard_hat", "shawl", "splintmail", "chasm_stone",
  "consecrated_wraps", "essence_distiller", "crellas_crozier", "hydras_breath".
  NOTE: These will initially FAIL because the test DB is seeded with conftest fixtures, not
  OpenDota data. That's correct -- they serve as a smoke test for production re-seed. Mark
  these with @pytest.mark.skip(reason="Requires live OpenDota re-seed data") for now, with
  a comment that they should be unskipped after the first production re-seed.

- test_cornucopia_removed: Assert data_cache.item_name_to_id("cornucopia") is None.
  This verifies Cornucopia was removed from the item catalog after re-seed.

- test_shivas_guard_cost_updated: Look up Shiva's Guard by internal_name "shivas_guard",
  assert item.cost == 4500. Use the conftest fixture (which we updated in Task 1).

- test_blade_mail_cost_updated: Look up Blade Mail by internal_name "blade_mail",
  assert item.cost == 2300. Use the conftest fixture.

**Class TestPatch741RulesIntegrity:**

- test_rules_no_cornucopia_reference: Import rules.py source text via inspect.getsource(),
  assert "cornucopia" not in source. Ensures no rule references a removed item.

- test_rules_no_stale_item_references: For every internal_name string literal in rules.py
  that's passed to self._item_id(), verify that data_cache.item_name_to_id(name) returns
  a valid ID. Use regex to extract all _item_id("xxx") calls from rules source.

- test_minimum_rule_count_741: Assert engine._rules length >= 22 (existing rules preserved).

**Class TestPatch741PromptBudget:**

- test_prompt_token_budget_with_741_hints: Import SYSTEM_PROMPT, estimate tokens as
  ceil(len / 3.5), assert < 5000. Same as existing test but grouped here for patch-specific
  validation.

All tests use the existing test_db_session and engine fixtures from conftest.
  </action>
  <verify>
    <automated>cd prismlab/backend && python -m pytest tests/test_patch_741.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>test_patch_741.py exists with 8+ tests covering new items, removed items, cost changes, rules integrity, and prompt budget; all non-skipped tests pass</done>
</task>

</tasks>

<verification>
- `cd prismlab/backend && python -m pytest tests/test_patch_741.py tests/test_rules.py tests/test_system_prompt.py -x -v` -- all tests pass
- seed.py line ~151 uses `session.merge()` not `session.add()`
- conftest.py Shiva's Guard cost is 4500
</verification>

<success_criteria>
1. seed.py HeroAbilityData bug fixed (merge instead of add)
2. Test fixtures reflect 7.41 costs
3. test_patch_741.py provides automated regression suite for patch data correctness
4. All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-patch-data-refresh/28-01-SUMMARY.md`
</output>
