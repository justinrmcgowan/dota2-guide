---
phase: 28-patch-data-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/backend/engine/rules.py
  - prismlab/backend/engine/prompts/system_prompt.py
  - prismlab/backend/tests/test_rules.py
autonomous: true
requirements: [PATCH-03, PATCH-04]
must_haves:
  truths:
    - "No rule references Cornucopia or any other removed 7.41 item"
    - "Shiva's Guard rule reasoning reflects its 7.41 cost accessibility for offlaners"
    - "System prompt contains concise 7.41 meta hints under 200 tokens"
    - "System prompt total token budget stays under 5000 tokens"
    - "Refresher Orb behavioral change is documented in system prompt meta hints"
  artifacts:
    - path: "prismlab/backend/engine/rules.py"
      provides: "Audited rules engine with 7.41-accurate references"
      min_lines: 900
    - path: "prismlab/backend/engine/prompts/system_prompt.py"
      provides: "System prompt with 7.41 meta hints section"
      contains: "Refresher Orb"
    - path: "prismlab/backend/tests/test_rules.py"
      provides: "Updated rule count test reflecting any new rules"
      contains: "test_minimum_rule_count"
  key_links:
    - from: "prismlab/backend/engine/prompts/system_prompt.py"
      to: "Claude API reasoning"
      via: "SYSTEM_PROMPT constant"
      pattern: "SYSTEM_PROMPT"
    - from: "prismlab/backend/engine/rules.py"
      to: "prismlab/backend/data/cache.py"
      via: "_item_id and _hero_id lookups"
      pattern: "self\\._item_id"
---

<objective>
Audit all 23 rules against 7.41 changes and add concise meta hints to the system prompt
for behavioral changes Claude can't infer from item data alone.

Purpose: Ensure the rules engine and system prompt reflect 7.41 game mechanics, so
recommendations are accurate for the current patch.
Output: Audited rules.py, updated system_prompt.py with meta hints, updated test_rules.py
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-patch-data-refresh/28-CONTEXT.md
@prismlab/backend/engine/rules.py
@prismlab/backend/engine/prompts/system_prompt.py
@prismlab/backend/tests/test_rules.py
@prismlab/backend/tests/test_system_prompt.py

<interfaces>
<!-- Key types the executor needs -->

From prismlab/backend/engine/rules.py:
```python
class RulesEngine:
    def _item_id(self, internal_name: str) -> int | None
    def _hero_id(self, name: str) -> int | None
    def _hero_name(self, hero_id: int) -> str
    # 22 rule methods in self._rules property list
    # Each returns list[RuleResult]
```

From prismlab/backend/engine/schemas.py:
```python
class RuleResult(BaseModel):
    item_id: int
    item_name: str
    reasoning: str
    phase: str  # "starting" | "laning" | "core" | "late_game" | "situational"
    priority: str  # "core" | "situational" | "luxury"
    counter_target: str | None = None
```

From prismlab/backend/engine/prompts/system_prompt.py:
```python
SYSTEM_PROMPT: str = """..."""  # Single constant, ~5500 chars, must stay under 5000 tokens
```

Current rules list (22 rules):
1. _magic_stick_rule
2. _quelling_blade_rule
3. _boots_rule
4. _raindrops_rule
5. _bkb_rule
6. _mkb_rule
7. _armor_rule (uses shivas_guard)
8. _force_staff_rule
9. _spirit_vessel_rule
10. _mana_sustain_rule
11. _dust_sentries_rule
12. _anti_heal_rule (uses shivas_guard)
13. _silver_edge_rule
14. _orchid_rule
15. _mekansm_rule
16. _pipe_rule
17. _halberd_rule
18. _ghost_scepter_rule
19. _euls_channel_rule
20. _lotus_linkens_rule
21. _dispel_counter_rule
22. _hex_root_escape_rule
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 23 rules against 7.41 and update system prompt</name>
  <files>prismlab/backend/engine/rules.py, prismlab/backend/engine/prompts/system_prompt.py</files>
  <action>
**Part A: Full rules audit (per D-04)**

Read every rule method in rules.py and check against 7.41 changes:

1. **_armor_rule** (line ~432): References shivas_guard for non-carry roles vs physical carries.
   The Shiva's Guard cost dropped from 5175g to 4500g in 7.41, making it more accessible.
   UPDATE the reasoning string for the Shiva's path to mention its improved accessibility:
   Change "Shiva's Guard reduces their attack speed aura and provides armor and int." to
   "Shiva's Guard (4500g in 7.41) reduces their attack speed and provides armor.
   More accessible timing for offlaners this patch."

2. **_anti_heal_rule** (line ~625): Uses shivas_guard for anti-heal. Shiva's is still valid
   for this. No internal_name change needed -- item ID resolves from DB. KEEP AS-IS.

3. **_orchid_rule** (line ~747): References orchid. Orchid's recipe changed in 7.41 (Cornucopia
   removed, now uses different components). The internal_name "orchid" is still valid. The
   rule recommends Orchid for its silence functionality, which is unchanged. KEEP AS-IS.

4. **All other rules**: Scan each for any reference to:
   - Cornucopia (none found -- confirmed via grep)
   - Refresher Orb (none found -- no rule recommends refresher)
   - Any removed item internal_names (none found)
   - Any cost hardcoded in reasoning strings (check all reasoning= strings)
   RESULT: No other rules need changes. The rules use _item_id() for lookups which resolve
   from DB, so cost changes are automatically picked up.

5. **New rules consideration (per D-05)**: Evaluate whether new 7.41 items warrant new rules:
   - Crella's Crozier (~4800g, anti-heal): Could add as alternative to Spirit Vessel in
     _anti_heal_rule for cores. However, the item is brand new and its meta hasn't settled.
     Claude will handle it through the LLM path. SKIP new rules for now -- Claude's Discretion.
   - Consecrated Wraps (~2600g, magic defense): Similar to Hood of Defiance niche.
     Not distinct enough from existing _pipe_rule and _bkb_rule to warrant a dedicated rule.
     SKIP.

**Part B: System prompt 7.41 meta hints (per D-06/D-07)**

Add a new section to the system prompt BEFORE the "## Output Fields" section. Keep it
under 200 tokens (~700 chars). Title: "## Patch 7.41 Notes"

Content:
```
## Patch 7.41 Notes
- Refresher Orb refreshes ABILITIES ONLY -- no longer refreshes item cooldowns. No double-BKB, double-Scythe, double-Manta.
- Bloodstone reworked: 12% spell damage amplification aura (1200 radius). Core pickup for spell-damage carries who fight with their team.
- Shiva's Guard cost reduced to 4500g (from 5175g). More accessible for offlaners and supports.
- Neutral T1 items available from minute 0 -- factor into starting item advice.
- Facets removed. Do not reference hero facets.
```

This is approximately 90 tokens -- well under the 200 token budget.
  </action>
  <verify>
    <automated>cd prismlab/backend && python -m pytest tests/test_system_prompt.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 22 rules audited, _armor_rule Shiva's reasoning updated, system prompt contains "Patch 7.41 Notes" section with 5 meta hints, token budget test passes</done>
</task>

<task type="auto">
  <name>Task 2: Update rule tests for 7.41 accuracy</name>
  <files>prismlab/backend/tests/test_rules.py</files>
  <action>
Update test_rules.py to validate 7.41 rule accuracy:

1. **TestRuleCount**: Update test_minimum_rule_count to assert >= 22 (unchanged if no new
   rules added, but verify the count matches the actual _rules property length).

2. **Add TestPatch741RuleAccuracy class** with these tests:

   - test_shivas_reasoning_mentions_741: Run _armor_rule with a physical carry opponent
     (e.g., PA id=12) as role=3 offlaner. Assert the Shiva's Guard result reasoning contains
     "4500" or "7.41" to verify the updated reasoning string.

   - test_no_cornucopia_in_rules: Import inspect, get source of RulesEngine class, assert
     "cornucopia" not in source.lower(). Ensures no stale item references.

   - test_no_refresher_orb_rule: Verify no rule returns a result with item_name containing
     "Refresher Orb". Run evaluate() with various lane opponents (Zeus id=22, Enigma id=33)
     as a core. The rules engine should NOT recommend Refresher Orb (it never did, but this
     is a regression guard for future rule additions).

3. **Add TestPatch741PromptHints class** (in test_rules.py or test_system_prompt.py -- put
   it in test_rules.py since it's 7.41-specific):

   - test_system_prompt_has_741_section: Assert "Patch 7.41" in SYSTEM_PROMPT.
   - test_refresher_orb_hint: Assert "Refresher Orb" in SYSTEM_PROMPT and "abilities only"
     in SYSTEM_PROMPT.lower() (or "abilities only" pattern).
   - test_bloodstone_hint: Assert "Bloodstone" in SYSTEM_PROMPT and "spell damage" in
     SYSTEM_PROMPT.lower().
   - test_facets_removed_hint: Assert "Facets removed" in SYSTEM_PROMPT or
     "facets" in SYSTEM_PROMPT.lower().

Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd prismlab/backend && python -m pytest tests/test_rules.py tests/test_system_prompt.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>test_rules.py has TestPatch741RuleAccuracy and TestPatch741PromptHints classes; all tests pass; no regressions in existing rule tests</done>
</task>

</tasks>

<verification>
- `cd prismlab/backend && python -m pytest tests/test_rules.py tests/test_system_prompt.py -x -v` -- all tests pass
- `grep -c "Patch 7.41" prismlab/backend/engine/prompts/system_prompt.py` returns 1
- `grep "cornucopia" prismlab/backend/engine/rules.py` returns nothing
- System prompt token estimate < 5000
</verification>

<success_criteria>
1. All 22 rules audited -- no stale references to removed items or outdated costs
2. _armor_rule Shiva's reasoning updated for 7.41 cost reduction
3. System prompt has "Patch 7.41 Notes" section with Refresher Orb, Bloodstone, Shiva's, neutral T1, facets hints
4. Total system prompt stays under 5000 token budget
5. All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-patch-data-refresh/28-02-SUMMARY.md`
</output>
