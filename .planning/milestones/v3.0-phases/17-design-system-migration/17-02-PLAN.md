---
phase: 17-design-system-migration
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - prismlab/frontend/src/App.tsx
  - prismlab/frontend/src/components/layout/Header.tsx
  - prismlab/frontend/src/components/layout/Sidebar.tsx
  - prismlab/frontend/src/components/layout/MainPanel.tsx
  - prismlab/frontend/src/components/layout/GsiStatusIndicator.tsx
  - prismlab/frontend/src/components/clock/GameClock.tsx
  - prismlab/frontend/src/components/draft/HeroPicker.tsx
  - prismlab/frontend/src/components/draft/HeroPortrait.tsx
  - prismlab/frontend/src/components/draft/HeroSlot.tsx
  - prismlab/frontend/src/components/draft/AllyPicker.tsx
  - prismlab/frontend/src/components/draft/OpponentPicker.tsx
  - prismlab/frontend/src/components/draft/RoleSelector.tsx
  - prismlab/frontend/src/components/draft/PlaystyleSelector.tsx
  - prismlab/frontend/src/components/draft/SideSelector.tsx
  - prismlab/frontend/src/components/draft/LaneSelector.tsx
  - prismlab/frontend/src/components/draft/LaneOpponentPicker.tsx
  - prismlab/frontend/src/components/draft/GetBuildButton.tsx
  - prismlab/frontend/src/utils/constants.ts
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - "The sidebar renders with surface-container-lowest (#0E0E0E) background"
    - "The main panel renders with surface (#131313) background"
    - "The header renders with surface-container-low (#1C1B1B) background"
    - "No visible 1px borders on layout containers"
    - "All toggle/radio buttons use new accent colors instead of cyan-accent"
    - "Section headings use font-display (Newsreader) and on-surface-variant color"
    - "The GetBuildButton uses primary-container (#B22222) background with gold outline on hover"
  artifacts:
    - path: "prismlab/frontend/src/App.tsx"
      provides: "Root layout with surface hierarchy"
      contains: "bg-surface"
    - path: "prismlab/frontend/src/components/layout/Sidebar.tsx"
      provides: "Sidebar with surface-container-lowest background"
      contains: "bg-surface-container-lowest"
    - path: "prismlab/frontend/src/components/layout/Header.tsx"
      provides: "Header with surface-container-low background"
      contains: "bg-surface-container-low"
  key_links:
    - from: "prismlab/frontend/src/App.tsx"
      to: "prismlab/frontend/src/styles/globals.css"
      via: "Tailwind utility classes referencing @theme tokens"
      pattern: "bg-surface"
---

<objective>
Migrate the layout shell (App, Header, Sidebar, MainPanel) and all draft-input components to the DESIGN.md token set. This covers the entire left sidebar, the header bar, and the main panel background -- the structural "bones" of the app.

Purpose: Establish the obsidian surface hierarchy across the app shell so all content nested inside inherits the correct tonal context.
Output: 18 files updated with new surface, text, border, and accent class names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/17-design-system-migration/17-01-SUMMARY.md

@prismlab/frontend/src/App.tsx
@prismlab/frontend/src/components/layout/Header.tsx
@prismlab/frontend/src/components/layout/Sidebar.tsx
@prismlab/frontend/src/components/layout/MainPanel.tsx
@prismlab/frontend/src/utils/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout shell (App, Header, Sidebar, MainPanel, GsiStatusIndicator, GameClock)</name>
  <files>
    prismlab/frontend/src/App.tsx,
    prismlab/frontend/src/components/layout/Header.tsx,
    prismlab/frontend/src/components/layout/Sidebar.tsx,
    prismlab/frontend/src/components/layout/MainPanel.tsx,
    prismlab/frontend/src/components/layout/GsiStatusIndicator.tsx,
    prismlab/frontend/src/components/clock/GameClock.tsx
  </files>
  <read_first>
    prismlab/frontend/src/App.tsx,
    prismlab/frontend/src/components/layout/Header.tsx,
    prismlab/frontend/src/components/layout/Sidebar.tsx,
    prismlab/frontend/src/components/layout/MainPanel.tsx,
    prismlab/frontend/src/components/layout/GsiStatusIndicator.tsx,
    prismlab/frontend/src/components/clock/GameClock.tsx,
    DESIGN.md
  </read_first>
  <action>
Apply the surface hierarchy mapping per D-06, D-07, D-08, and the No-Line Rule.

**App.tsx** (root shell):
- `bg-bg-primary` -> `bg-surface` (per D-07: main panel = surface #131313)
- `text-gray-100` -> `text-on-surface` (#E5E2E1, per DESIGN.md "Don't use 100% white")
- Keep `font-body` (already mapped to Manrope in @theme)

**Header.tsx** (per D-08: surface-container-low #1C1B1B):
- `bg-bg-secondary` -> `bg-surface-container-low`
- REMOVE `border-b border-bg-elevated` (No-Line Rule -- depth comes from bg color shift)
- `text-cyan-accent` (Prismlab title) -> `text-secondary` (#FFDB3C gold, per D-13: Gold for headlines)
- Change Prismlab title to `font-display` instead of `font-body` (per D-11: Newsreader for display/headlines)
- Add `tracking-tight` to the title (per D-11: tight letter-spacing -0.02em, tracking-tight is -0.025em)
- SVG prism gradient: replace `#00d4ff` with `#FFB4AC` (primary) and keep `#6aff97` (radiant) -- retheme from cyan to crimson+radiant
- Settings button: `hover:text-cyan-accent` -> `hover:text-primary`
- `text-text-muted` -> `text-on-surface-variant` for freshness label and settings icon default

**Sidebar.tsx** (per D-06: surface-container-lowest #0E0E0E):
- `bg-bg-secondary` -> `bg-surface-container-lowest`
- REMOVE `border-r border-bg-elevated` (No-Line Rule)
- All section headings `text-gray-400` -> `text-on-surface-variant` (#E2BEBA)
- Bottom CTA area: REMOVE `border-t border-bg-elevated` (No-Line Rule). Instead add `bg-surface-container-low` to create visual separation via tonal shift.

**MainPanel.tsx**:
- `bg-bg-primary` -> `bg-surface`
- Hero header: `text-gray-200` -> `text-on-surface`, hero name should use `font-display` since it's a headline
- `text-gray-500` subtitle -> `text-on-surface-variant`
- Hero image: `rounded` -> remove (0px corners from @theme handles this). KEEP `rounded` on hero images as a functional exception per D-05 (avatar/portrait images use roundedness to soften harsh rectangular edges from Steam CDN).
- Empty state: `text-gray-500` -> `text-on-surface-variant`

**GsiStatusIndicator.tsx**:
- Keep `bg-radiant`, `bg-gray-500`, `bg-dire` for dot colors (game colors preserved)
- Keep `rounded-full` on the status dot -- per D-05: functional roundedness exception (status indicator dots)
- `text-text-muted` -> `text-on-surface-variant`

**GameClock.tsx**:
- `text-cyan-accent` -> `text-secondary` (#FFDB3C gold, game clock is a headline/label)
- `font-mono` -> `font-body` with tabular-nums (Manrope with tnum feature already set in @theme)
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "bg-surface-container-lowest" src/components/layout/Sidebar.tsx && grep -c "bg-surface-container-low" src/components/layout/Header.tsx && grep -c "bg-surface" src/components/layout/MainPanel.tsx && grep -c "bg-surface" src/App.tsx && grep -c "border-b" src/components/layout/Header.tsx | grep "^0$" && npx tsc -b --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx: contains `bg-surface` and `text-on-surface`, does NOT contain `bg-bg-primary` or `text-gray-100`
    - Header.tsx: contains `bg-surface-container-low`, does NOT contain `border-b`, contains `text-secondary` for title, contains `font-display` for title
    - Sidebar.tsx: contains `bg-surface-container-lowest`, does NOT contain `border-r border-bg-elevated`, section headings use `text-on-surface-variant`
    - MainPanel.tsx: contains `bg-surface`, does NOT contain `bg-bg-primary`
    - GsiStatusIndicator.tsx: status dot retains `rounded-full`, text uses `text-on-surface-variant`
    - GameClock.tsx: contains `text-secondary`, does NOT contain `text-cyan-accent` or `font-mono`
  </acceptance_criteria>
  <done>Layout shell migrated: Sidebar is surface-container-lowest (#0E0E0E), Header is surface-container-low (#1C1B1B), MainPanel is surface (#131313). No 1px borders on layout containers. Prismlab title uses font-display (Newsreader) in gold.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all draft-input components and constants</name>
  <files>
    prismlab/frontend/src/components/draft/HeroPicker.tsx,
    prismlab/frontend/src/components/draft/HeroPortrait.tsx,
    prismlab/frontend/src/components/draft/HeroSlot.tsx,
    prismlab/frontend/src/components/draft/AllyPicker.tsx,
    prismlab/frontend/src/components/draft/OpponentPicker.tsx,
    prismlab/frontend/src/components/draft/RoleSelector.tsx,
    prismlab/frontend/src/components/draft/PlaystyleSelector.tsx,
    prismlab/frontend/src/components/draft/SideSelector.tsx,
    prismlab/frontend/src/components/draft/LaneSelector.tsx,
    prismlab/frontend/src/components/draft/LaneOpponentPicker.tsx,
    prismlab/frontend/src/components/draft/GetBuildButton.tsx,
    prismlab/frontend/src/utils/constants.ts
  </files>
  <read_first>
    prismlab/frontend/src/components/draft/RoleSelector.tsx,
    prismlab/frontend/src/components/draft/PlaystyleSelector.tsx,
    prismlab/frontend/src/components/draft/SideSelector.tsx,
    prismlab/frontend/src/components/draft/LaneSelector.tsx,
    prismlab/frontend/src/components/draft/GetBuildButton.tsx,
    prismlab/frontend/src/components/draft/HeroPicker.tsx,
    prismlab/frontend/src/components/draft/HeroPortrait.tsx,
    prismlab/frontend/src/components/draft/HeroSlot.tsx,
    prismlab/frontend/src/components/draft/LaneOpponentPicker.tsx,
    prismlab/frontend/src/components/draft/AllyPicker.tsx,
    prismlab/frontend/src/components/draft/OpponentPicker.tsx,
    prismlab/frontend/src/utils/constants.ts,
    DESIGN.md
  </read_first>
  <action>
Migrate all sidebar draft-input components to the DESIGN.md design system. Apply consistently across all toggle/radio button components.

**COMMON PATTERN for toggle/radio buttons** (RoleSelector, PlaystyleSelector, LaneSelector, LaneOpponentPicker, DamageProfileInput presets):

Active state (old): `bg-cyan-accent/20 text-cyan-accent border-cyan-accent`
Active state (new): `bg-primary-container/20 text-primary border-primary/40`
(Per DESIGN.md: primary_container #B22222 for active affordance, primary #FFB4AC for active text)

Inactive state (old): `bg-bg-elevated text-gray-400 border-bg-elevated hover:text-gray-200`
Inactive state (new): `bg-surface-container-high text-on-surface-variant border-surface-container-high hover:text-on-surface`
(Per DESIGN.md: surface-container-high #2B2A29 for interactive resting state)

REMOVE `rounded-md` from all toggle buttons (0px corners from @theme). The border is acceptable on interactive elements per DESIGN.md Ghost Border principle -- but change to `outline outline-1 outline-outline-variant/15` on inactive, `outline outline-1 outline-primary/40` on active. Or simpler: keep the `border` pattern but update colors.

DECISION: Keep `border` utility for button toggle indicators since they communicate state, but use ghost-border colors. Active: `border border-primary/40`, Inactive: `border border-outline-variant/15`.

**RoleSelector.tsx, PlaystyleSelector.tsx, LaneSelector.tsx**:
- Apply common toggle pattern above
- Remove `rounded-md` class

**SideSelector.tsx**:
- Radiant active: `bg-radiant/20 text-radiant border-radiant` -> KEEP (game colors preserved per design)
- Dire active: `bg-dire/20 text-dire border-dire` -> KEEP
- Inactive state: apply new inactive pattern
- Remove `rounded-md`

**LaneOpponentPicker.tsx**:
- Active: apply common toggle pattern
- Inactive: apply common inactive pattern
- Remove `rounded-md`
- Hero icon: `rounded-full` on tiny hero icon in lane opponent pill -- per D-05 functional exception (avatar/portrait)

**GetBuildButton.tsx** (per DESIGN.md "Blade" pattern, D-15):
- Remove `rounded-lg`
- Enabled state: `bg-cyan-accent text-bg-primary` -> `bg-primary-container text-on-surface font-display` (Blade pattern: primary_container background #B22222)
- Hover: remove `hover:brightness-110 shadow-[0_0_15px_rgba(0,212,255,0.3)]` -> add `hover:outline hover:outline-1 hover:outline-[#AA8986] shadow-glow-active` (Blade hover: gold leaf outline + glow)
- Disabled state: `bg-bg-elevated text-gray-500` -> `bg-surface-container-high text-on-surface-variant/40`
- Loading text "Analyzing..." stays, pulse animation stays

**HeroPicker.tsx**:
- Search input: `bg-bg-elevated border border-bg-elevated rounded-lg` -> `bg-surface-container-lowest border-b border-outline-variant/15` (Sacrificial Table pattern: recessed + underline only)
- Remove `rounded-lg` from input
- `focus:ring-2 focus:ring-cyan-accent` -> `focus:ring-1 focus:ring-primary` (keep focus visible for accessibility)
- Dropdown: `bg-bg-secondary border border-bg-elevated rounded-lg` -> `bg-surface-container-low` REMOVE border and rounded-lg
- `text-gray-100` -> `text-on-surface`
- `text-gray-400` loading text -> `text-on-surface-variant`

**HeroPortrait.tsx**:
- `rounded-md` on container -> remove
- `hover:bg-bg-elevated` -> `hover:bg-surface-container-high`
- `ring-2 ring-cyan-accent` (selected) -> `ring-1 ring-secondary-fixed` (gold highlight for selected hero)
- `text-gray-100` name -> `text-on-surface`
- Hero image: keep `rounded` per D-05 (avatar functional exception)
- Attribute dot: keep `rounded-full` per D-05 (status indicator dot)

**HeroSlot.tsx**:
- Empty slot: `rounded-full border-2 border-dashed border-gray-500` -> keep `rounded-full` per D-05 (slot placeholder is circular affordance), update `border-gray-500` to `border-outline-variant`
- `hover:border-gray-300 hover:text-gray-300` -> `hover:border-on-surface-variant hover:text-on-surface-variant`
- Filled: hero image keep `rounded-full` per D-05
- Hover overlay: `rounded-full bg-black/70` -> keep `rounded-full` per D-05
- `text-gray-200 hover:text-white` -> `text-on-surface hover:text-on-surface`

**AllyPicker.tsx**:
- `border-l-2 border-radiant` -> KEEP (game color accent, meaningful)

**OpponentPicker.tsx**:
- `border-l-2 border-dire` -> KEEP (game color accent, meaningful)

**constants.ts** (LANE_RESULT_OPTIONS):
- "even" option: `text-cyan-accent` -> `text-primary`, `bg-cyan-accent/20` -> `bg-primary/20`, `border-cyan-accent` -> `border-primary`
- "won" and "lost" options: KEEP (already using game colors radiant/dire)
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "cyan-accent" src/components/draft/RoleSelector.tsx src/components/draft/PlaystyleSelector.tsx src/components/draft/LaneSelector.tsx src/components/draft/GetBuildButton.tsx src/components/draft/HeroPicker.tsx src/components/draft/HeroPortrait.tsx src/components/draft/LaneOpponentPicker.tsx src/utils/constants.ts 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero instances of `cyan-accent` in any draft component or constants.ts
    - RoleSelector, PlaystyleSelector, LaneSelector: active state uses `text-primary` and `bg-primary-container/20`
    - GetBuildButton: enabled state uses `bg-primary-container` and `text-on-surface`
    - HeroPicker search input: uses `bg-surface-container-lowest`, no `rounded-lg`
    - HeroPortrait: selected state uses `ring-secondary-fixed` instead of `ring-cyan-accent`
    - constants.ts LANE_RESULT_OPTIONS "even": uses `text-primary` instead of `text-cyan-accent`
    - SideSelector: radiant/dire active states UNCHANGED (game colors preserved)
    - npm run build succeeds
  </acceptance_criteria>
  <done>All 11 draft-input components and constants.ts migrated to DESIGN.md tokens. Toggle buttons use primary/primary-container accents. GetBuildButton follows the Blade pattern. HeroPicker inputs follow Sacrificial Table pattern. Game colors (radiant/dire) preserved. No cyan-accent references remain in draft components.</done>
</task>

</tasks>

<verification>
1. `cd prismlab/frontend && npm run build` succeeds
2. `grep -r "cyan-accent" src/components/draft/ src/components/layout/ src/components/clock/ src/App.tsx src/utils/constants.ts` returns zero matches
3. `grep "bg-surface-container-lowest" src/components/layout/Sidebar.tsx` returns match
4. `grep "bg-surface-container-low" src/components/layout/Header.tsx` returns match
5. `grep "bg-primary-container" src/components/draft/GetBuildButton.tsx` returns match
</verification>

<success_criteria>
- All layout containers use correct surface hierarchy per D-06/D-07/D-08
- No 1px borders on layout containers (No-Line Rule)
- All interactive elements use primary/secondary accents instead of cyan
- Game colors (radiant/dire) preserved on SideSelector and ally/opponent pickers
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-migration/17-02-SUMMARY.md`
</output>
