---
phase: 17-design-system-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/frontend/package.json
  - prismlab/frontend/src/main.tsx
  - prismlab/frontend/src/styles/globals.css
autonomous: true
requirements: [DESIGN-01, DESIGN-02]
must_haves:
  truths:
    - "The app renders with Newsreader for display text and Manrope for body text"
    - "All Tailwind color utilities resolve to the obsidian surface hierarchy"
    - "No external CDN fonts are loaded at runtime"
    - "All border-radius utilities resolve to 0px"
    - "Game colors (radiant/dire) and attribute colors are preserved unchanged"
  artifacts:
    - path: "prismlab/frontend/src/styles/globals.css"
      provides: "Complete @theme block with DESIGN.md tokens"
      contains: "--color-surface: #131313"
    - path: "prismlab/frontend/src/main.tsx"
      provides: "Variable font imports replacing static weight imports"
      contains: "@fontsource-variable/newsreader"
  key_links:
    - from: "prismlab/frontend/src/main.tsx"
      to: "node_modules/@fontsource-variable/newsreader"
      via: "CSS import"
      pattern: "fontsource-variable/newsreader"
    - from: "prismlab/frontend/src/styles/globals.css"
      to: "Tailwind utility generation"
      via: "@theme directive"
      pattern: "--color-surface"
---

<objective>
Install the new font packages, replace the @theme block in globals.css with all DESIGN.md tokens, and swap font imports in main.tsx. This single-file token swap covers ~60% of the visual migration instantly because every Tailwind utility referencing these CSS custom properties updates globally.

Purpose: Foundation layer -- every subsequent plan depends on the new token palette being in place.
Output: globals.css with full DESIGN.md token set, main.tsx with variable font imports, package.json with new font dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md

@prismlab/frontend/src/styles/globals.css
@prismlab/frontend/src/main.tsx
@prismlab/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap font packages and update imports</name>
  <files>prismlab/frontend/package.json, prismlab/frontend/src/main.tsx</files>
  <read_first>prismlab/frontend/package.json, prismlab/frontend/src/main.tsx</read_first>
  <action>
Per D-03: Install @fontsource-variable/newsreader and @fontsource-variable/manrope. Remove @fontsource/inter and @fontsource/jetbrains-mono.

1. In prismlab/frontend/ directory, run:
   npm uninstall @fontsource/inter @fontsource/jetbrains-mono
   npm install @fontsource-variable/newsreader @fontsource-variable/manrope

2. In main.tsx, replace the 6 static font CSS imports:
   DELETE:
     import "@fontsource/inter/400.css";
     import "@fontsource/inter/500.css";
     import "@fontsource/inter/600.css";
     import "@fontsource/inter/700.css";
     import "@fontsource/jetbrains-mono/400.css";
     import "@fontsource/jetbrains-mono/500.css";
   ADD:
     import "@fontsource-variable/newsreader";
     import "@fontsource-variable/manrope";

Two imports replace six. Variable fonts include all weights in a single file each.
  </action>
  <verify>
    <automated>cd prismlab/frontend && node -e "const p=require('./package.json'); const d=p.dependencies; if(!d['@fontsource-variable/newsreader'] || !d['@fontsource-variable/manrope']) throw 'missing new fonts'; if(d['@fontsource/inter'] || d['@fontsource/jetbrains-mono']) throw 'old fonts remain'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains @fontsource-variable/newsreader and @fontsource-variable/manrope in dependencies
    - package.json does NOT contain @fontsource/inter or @fontsource/jetbrains-mono
    - main.tsx contains exactly `import "@fontsource-variable/newsreader"` and `import "@fontsource-variable/manrope"`
    - main.tsx does NOT contain any `@fontsource/inter` or `@fontsource/jetbrains-mono` imports
  </acceptance_criteria>
  <done>Font packages swapped: 2 variable font packages replace 2 static packages. main.tsx has 2 imports instead of 6.</done>
</task>

<task type="auto">
  <name>Task 2: Replace @theme block with full DESIGN.md token set</name>
  <files>prismlab/frontend/src/styles/globals.css</files>
  <read_first>prismlab/frontend/src/styles/globals.css, DESIGN.md</read_first>
  <action>
Per D-01, D-02: Replace the entire @theme block in globals.css. The new block must include:

1. KEEP the @import "tailwindcss"; line at the top (line 1).

2. REPLACE the entire @theme { ... } block with:

```css
@theme {
  /* === Surface Hierarchy (DESIGN.md Section 2) === */
  --color-surface: #131313;
  --color-surface-dim: #131313;
  --color-surface-bright: #3A3939;
  --color-surface-container-lowest: #0E0E0E;
  --color-surface-container-low: #1C1B1B;
  --color-surface-container: #201F1F;
  --color-surface-container-high: #2B2A29;
  --color-surface-container-highest: #353534;

  /* === Primary (Crimson) === */
  --color-primary: #FFB4AC;
  --color-primary-container: #B22222;

  /* === Secondary (Gold) === */
  --color-secondary: #FFDB3C;
  --color-secondary-container: #FFDB3C;
  --color-secondary-fixed: #FFE16D;
  --color-secondary-fixed-dim: #D4A017;

  /* === Tertiary (Slate) === */
  --color-tertiary: #8DA4B8;
  --color-tertiary-container: #4E5E6D;

  /* === Text / Outline === */
  --color-on-surface: #E5E2E1;
  --color-on-surface-variant: #E2BEBA;
  --color-outline-variant: #5A403E;

  /* === Game Colors (PRESERVED) === */
  --color-radiant: #6AFF97;
  --color-dire: #FF5555;

  /* === Attribute Colors (PRESERVED) === */
  --color-attr-str: oklch(68% 0.19 25);
  --color-attr-agi: oklch(75% 0.18 145);
  --color-attr-int: oklch(70% 0.15 250);
  --color-attr-all: oklch(80% 0.1 90);

  /* === DEPRECATED — remove after Plan 05 audit confirms zero refs === */
  --color-cyan-accent: #FFB4AC;
  --color-bg-primary: #131313;
  --color-bg-secondary: #1C1B1B;
  --color-bg-elevated: #2B2A29;
  --color-text-muted: #E2BEBA;

  /* === Typography (D-11, D-12) === */
  --font-display: "Newsreader Variable", Georgia, "Times New Roman", serif;
  --font-display--font-variation-settings: "opsz" 32;
  --font-body: "Manrope Variable", system-ui, -apple-system, sans-serif;
  --font-body--font-feature-settings: "tnum";
  --font-stats: "Manrope Variable", system-ui, -apple-system, sans-serif;

  /* === Ambient Glow Shadows (D-15) === */
  --shadow-glow: 0 0 32px rgb(255 180 172 / 0.05);
  --shadow-glow-gold: 0 0 24px rgb(255 219 60 / 0.08);
  --shadow-glow-active: 0 0 16px rgb(178 34 34 / 0.15);

  /* === Disable all rounded corners (DESIGN.md Section 4) === */
  --radius-xs: 0px;
  --radius-sm: 0px;
  --radius-md: 0px;
  --radius-lg: 0px;
  --radius-xl: 0px;
  --radius-2xl: 0px;
  --radius-3xl: 0px;
  --radius-4xl: 0px;
}
```

CRITICAL DECISIONS:
- The deprecated color aliases (cyan-accent, bg-primary, bg-secondary, bg-elevated, text-muted) are KEPT temporarily. They now map to the new palette's nearest equivalents. This prevents invisible text per Pitfall 6. They will be removed in Plan 05 after a grep confirms zero references to old class names.
- --font-stats is remapped to Manrope Variable (per DESIGN.md: Manrope handles all body/stats with tabular-nums via font-feature-settings "tnum").
- All --radius-* tokens explicitly set to 0px to disable rounded corners globally. The --radius-*: initial syntax may not work reliably in Tailwind v4; explicit 0px values are safer.
- Game colors (radiant/dire) and attribute colors are preserved unchanged.
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "surface-container-lowest" src/styles/globals.css && grep -c "Newsreader Variable" src/styles/globals.css && grep -c "shadow-glow" src/styles/globals.css && grep -c "radius-sm: 0px" src/styles/globals.css && npx tsc -b --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css @theme block contains --color-surface: #131313
    - globals.css @theme block contains --color-surface-container-lowest: #0E0E0E
    - globals.css @theme block contains --color-primary: #FFB4AC
    - globals.css @theme block contains --color-secondary: #FFDB3C
    - globals.css @theme block contains --color-on-surface: #E5E2E1
    - globals.css @theme block contains --font-display: "Newsreader Variable"
    - globals.css @theme block contains --font-body: "Manrope Variable"
    - globals.css @theme block contains --shadow-glow: 0 0 32px rgb(255 180 172 / 0.05)
    - globals.css @theme block contains --radius-sm: 0px (and all other radius tokens set to 0px)
    - globals.css @theme block STILL contains --color-radiant: #6AFF97 and --color-dire: #FF5555
    - globals.css @theme block STILL contains deprecated aliases (cyan-accent, bg-primary, etc.) mapping to new values
    - npm run build succeeds (no broken token references)
  </acceptance_criteria>
  <done>The @theme block in globals.css contains the complete DESIGN.md token set: 8 surface tones, crimson/gold/slate accent palette, Newsreader + Manrope fonts, ambient glow shadows, 0px radius overrides, and preserved game/attribute colors. Deprecated aliases remain as bridge tokens.</done>
</task>

</tasks>

<verification>
1. `cd prismlab/frontend && npm run build` completes without errors
2. grep confirms new tokens present: `grep "surface-container" src/styles/globals.css`
3. grep confirms fonts present: `grep "Newsreader Variable" src/styles/globals.css`
4. grep confirms deprecated aliases still present: `grep "cyan-accent" src/styles/globals.css`
5. grep confirms old fonts removed from main.tsx: `grep -c "fontsource/inter" src/main.tsx` returns 0
</verification>

<success_criteria>
- New font packages installed, old ones removed
- @theme block contains all DESIGN.md tokens
- Deprecated aliases bridge existing components to new palette
- Build succeeds
- All Tailwind utilities referencing custom properties now resolve to new values
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-migration/17-01-SUMMARY.md`
</output>
