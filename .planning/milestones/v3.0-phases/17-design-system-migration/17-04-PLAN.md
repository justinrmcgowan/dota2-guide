---
phase: 17-design-system-migration
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - prismlab/frontend/src/components/settings/SettingsPanel.tsx
  - prismlab/frontend/src/components/screenshot/ScreenshotParser.tsx
  - prismlab/frontend/src/components/screenshot/ParsedHeroRow.tsx
  - prismlab/frontend/src/components/screenshot/ItemEditPicker.tsx
  - prismlab/frontend/src/components/toast/AutoRefreshToast.tsx
  - prismlab/frontend/src/components/timeline/ErrorBanner.tsx
autonomous: true
requirements: [DESIGN-04, DESIGN-06]
must_haves:
  truths:
    - "SettingsPanel backdrop has blood-glass effect (primary_container at 20-40% opacity with backdrop-blur 12px)"
    - "ScreenshotParser modal has surface-container-highest background with ambient crimson glow shadow"
    - "AutoRefreshToast has blood-glass overlay effect instead of solid bg-bg-secondary"
    - "ErrorBanner uses tonal crimson surface instead of amber borders"
    - "No rounded corners on floating elements except documented functional exceptions"
  artifacts:
    - path: "prismlab/frontend/src/components/settings/SettingsPanel.tsx"
      provides: "Blood-glass settings slide-over panel"
      contains: "backdrop-blur"
    - path: "prismlab/frontend/src/components/screenshot/ScreenshotParser.tsx"
      provides: "Blood-glass screenshot modal"
      contains: "shadow-glow"
  key_links:
    - from: "prismlab/frontend/src/components/settings/SettingsPanel.tsx"
      to: "prismlab/frontend/src/styles/globals.css"
      via: "shadow-glow and backdrop-blur tokens"
      pattern: "shadow-glow"
---

<objective>
Migrate all floating/overlay elements (modals, panels, toasts, banners) to the DESIGN.md design system with blood-glass overlays, ambient crimson glow shadows, and the surface-container-highest tonal level per D-09, D-15, D-16, D-17.

Purpose: Floating elements are the most visually dramatic surfaces -- blood-glass and crimson aura make them feel like "heavy slabs hovering over the abyss."
Output: 6 files updated with blood-glass, ambient glow, and surface-container-highest treatments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/17-design-system-migration/17-01-SUMMARY.md

@prismlab/frontend/src/components/settings/SettingsPanel.tsx
@prismlab/frontend/src/components/screenshot/ScreenshotParser.tsx
@prismlab/frontend/src/components/toast/AutoRefreshToast.tsx
@prismlab/frontend/src/components/timeline/ErrorBanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SettingsPanel and ScreenshotParser (blood-glass modals)</name>
  <files>
    prismlab/frontend/src/components/settings/SettingsPanel.tsx,
    prismlab/frontend/src/components/screenshot/ScreenshotParser.tsx,
    prismlab/frontend/src/components/screenshot/ParsedHeroRow.tsx,
    prismlab/frontend/src/components/screenshot/ItemEditPicker.tsx
  </files>
  <read_first>
    prismlab/frontend/src/components/settings/SettingsPanel.tsx,
    prismlab/frontend/src/components/screenshot/ScreenshotParser.tsx,
    prismlab/frontend/src/components/screenshot/ParsedHeroRow.tsx,
    prismlab/frontend/src/components/screenshot/ItemEditPicker.tsx,
    DESIGN.md
  </read_first>
  <action>
Per D-09: Floating elements use surface-container-highest (#353534) with ambient crimson glow and blood-glass overlay. Per D-17: blood-glass = primary_container (#B22222) at 20-40% opacity with backdrop-blur 12px.

**SettingsPanel.tsx** (per D-09, D-17: blood-glass slide-over):
- Backdrop: `bg-black/50` -> `bg-primary-container/25 backdrop-blur-md` (blood-glass effect: #B22222 at 25% with 12px blur)
- Panel: `bg-bg-secondary border-l border-bg-elevated shadow-2xl` -> `bg-surface-container-highest shadow-glow` REMOVE `border-l border-bg-elevated` (No-Line Rule). Add ambient crimson glow shadow.
- Title: `text-cyan-accent` -> `text-secondary font-display` (gold headline)
- Close button: `text-text-muted hover:text-gray-100` -> `text-on-surface-variant hover:text-on-surface`
- Section heading: `text-gray-300` -> `text-on-surface`
- Labels: `text-text-muted` -> `text-on-surface-variant`
- Input field (Sacrificial Table pattern per DESIGN.md Section 5):
  - `bg-bg-primary border border-bg-elevated rounded` -> `bg-surface-container-lowest border-b border-outline-variant/15` REMOVE `rounded`, remove top/side borders, keep only bottom border for editorial underline feel
  - `focus:border-cyan-accent` -> `focus:border-primary`
  - `text-gray-100` -> `text-on-surface`
  - `placeholder-gray-600` -> `placeholder-on-surface-variant/40`
- Port display: same Sacrificial Table pattern: `bg-bg-primary border border-bg-elevated rounded text-gray-400` -> `bg-surface-container-lowest border-b border-outline-variant/15 text-on-surface-variant` REMOVE `rounded`
- Download button (Blade pattern):
  - `bg-cyan-accent/20 text-cyan-accent border border-cyan-accent/30 rounded` -> `bg-primary-container text-on-surface border border-outline-variant/15` REMOVE `rounded`
  - `hover:bg-cyan-accent/30` -> `hover:outline hover:outline-1 hover:outline-[#AA8986]`
  - `disabled:opacity-40 disabled:cursor-not-allowed` -> keep
- Code blocks: `text-cyan-accent bg-bg-primary px-1 rounded` -> `text-primary bg-surface-container-lowest px-1`
- Instruction text: `text-text-muted` -> `text-on-surface-variant`
- Step strong text: `text-gray-300` -> `text-on-surface`
- Green span: `text-radiant` -> KEEP (game color)
- Setup heading: `text-gray-300` -> `text-on-surface`
- Sub-section heading: `text-gray-300` -> `text-on-surface`

**ScreenshotParser.tsx** (per D-09, D-17: blood-glass modal):
- Backdrop: `bg-black/60` -> `bg-primary-container/30 backdrop-blur-md` (blood-glass with stronger opacity for modal)
- Modal container: `bg-bg-secondary rounded-lg shadow-2xl` -> `bg-surface-container-highest shadow-glow` REMOVE `rounded-lg`
- Header border: `border-b border-bg-elevated` -> REMOVE (No-Line Rule)
- Title: `text-cyan-accent` -> `text-secondary font-display`
- Close button: `text-gray-500 hover:text-gray-100` -> `text-on-surface-variant hover:text-on-surface`
- Upload zone: `border-2 border-dashed border-gray-600 rounded-lg` -> `border-2 border-dashed border-outline-variant` REMOVE `rounded-lg`
- `hover:border-cyan-accent` -> `hover:border-primary`
- Upload icon: `text-gray-500` -> `text-on-surface-variant`
- Upload text: `text-gray-400` -> `text-on-surface-variant`, `text-gray-600` -> `text-on-surface-variant/50`
- Screenshot thumbnail: `rounded-lg border border-bg-elevated` -> REMOVE `rounded-lg` and border
- Loading spinner: `border-2 border-cyan-accent border-t-transparent rounded-full` -> `border-2 border-primary border-t-transparent rounded-full` (keep rounded-full per D-05: functional spinner)
- Loading text: `text-gray-400` -> `text-on-surface-variant`
- Error container: `bg-dire/10 border border-dire/30 rounded-lg` -> `bg-dire/10 border border-dire/30` REMOVE `rounded-lg`
- Error text: `text-dire` -> KEEP
- Retry link: `text-cyan-accent hover:text-cyan-accent/80` -> `text-primary hover:text-primary/80`
- Empty state: `text-gray-500` -> `text-on-surface-variant`
- Latency: `text-gray-600` -> `text-on-surface-variant/50`
- Footer border: `border-t border-bg-elevated` -> REMOVE (No-Line Rule)
- Cancel button: `text-gray-400 hover:text-gray-100 bg-bg-elevated rounded-md` -> `text-on-surface-variant hover:text-on-surface bg-surface-container-high`
- Apply button: `text-bg-primary bg-cyan-accent rounded-md hover:bg-cyan-accent/90` -> `text-on-surface bg-primary-container hover:outline hover:outline-1 hover:outline-[#AA8986]` (Blade pattern)

**ParsedHeroRow.tsx**:
- Container: `bg-bg-elevated rounded-lg` -> `bg-surface-container-high` REMOVE `rounded-lg`
- Hero name: `text-gray-100` -> `text-on-surface`
- KDA: `text-gray-400` -> `text-on-surface-variant`
- Level: `text-gray-500` -> `text-on-surface-variant/70`
- Hero portrait/items: `rounded` -> per D-05 keep on portraits and item images
- Low confidence ring: `ring-2 ring-orange-400` -> keep (functional: confidence indicator)
- Medium confidence ring: `ring-1 ring-yellow-500/50` -> keep
- Low confidence badge: `bg-orange-400 text-black rounded-full` -> keep `rounded-full` per D-05 (badge dot)
- Remove button hover: `bg-dire` -> KEEP (game color)
- Add item button: `rounded border border-dashed border-gray-600 text-gray-500 hover:border-cyan-accent hover:text-cyan-accent` -> `border border-dashed border-outline-variant text-on-surface-variant hover:border-primary hover:text-primary`
- Remove hero button: `text-gray-600 hover:text-dire` -> `text-on-surface-variant/50 hover:text-dire`
- Fallback portrait div: `rounded bg-gray-700` -> `bg-surface-container`

**ItemEditPicker.tsx**:
- Container: `bg-bg-secondary border border-bg-elevated rounded-lg shadow-xl` -> `bg-surface-container-highest shadow-glow` REMOVE `rounded-lg` and `border`
- Search input: `bg-bg-primary border-b border-bg-elevated text-gray-100 placeholder-gray-600 focus:border-cyan-accent` -> `bg-surface-container-lowest border-b border-outline-variant/15 text-on-surface placeholder-on-surface-variant/40 focus:border-primary`
- List item button: `text-gray-200 hover:bg-bg-elevated` -> `text-on-surface hover:bg-surface-container-high`
- No results: `text-gray-500` -> `text-on-surface-variant`
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "cyan-accent" src/components/settings/SettingsPanel.tsx src/components/screenshot/ScreenshotParser.tsx src/components/screenshot/ParsedHeroRow.tsx src/components/screenshot/ItemEditPicker.tsx 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero instances of `cyan-accent` across all 4 files
    - SettingsPanel.tsx: backdrop uses `backdrop-blur-md` and `bg-primary-container/25`, panel uses `bg-surface-container-highest shadow-glow`
    - ScreenshotParser.tsx: backdrop uses `backdrop-blur-md`, modal uses `bg-surface-container-highest shadow-glow`, no `rounded-lg`
    - ParsedHeroRow.tsx: uses `bg-surface-container-high`, hero portraits keep `rounded`
    - ItemEditPicker.tsx: uses `bg-surface-container-highest shadow-glow`, no `rounded-lg`
    - SettingsPanel inputs: follow Sacrificial Table pattern (bg-surface-container-lowest, bottom border only)
    - npm run build succeeds
  </acceptance_criteria>
  <done>SettingsPanel and ScreenshotParser modals use blood-glass backdrop (primary_container with backdrop-blur-md). Panels use surface-container-highest with ambient crimson glow shadow. All inputs follow Sacrificial Table pattern. ParsedHeroRow and ItemEditPicker migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AutoRefreshToast and ErrorBanner</name>
  <files>
    prismlab/frontend/src/components/toast/AutoRefreshToast.tsx,
    prismlab/frontend/src/components/timeline/ErrorBanner.tsx
  </files>
  <read_first>
    prismlab/frontend/src/components/toast/AutoRefreshToast.tsx,
    prismlab/frontend/src/components/timeline/ErrorBanner.tsx,
    DESIGN.md
  </read_first>
  <action>
Per D-17 and Pitfall 13: Toast uses solid background (not backdrop-blur, since it updates frequently). ErrorBanner uses tonal crimson instead of amber.

**AutoRefreshToast.tsx**:
- Container: `rounded-lg border border-bg-elevated bg-bg-secondary p-4 shadow-xl` -> `bg-surface-container-highest p-4 shadow-glow` REMOVE `rounded-lg` and `border border-bg-elevated`
- Lightning icon: `text-cyan-accent` -> `text-secondary` (gold, since this is a positive notification)
- Title: `text-cyan-accent` -> `text-secondary`
- Body: `text-gray-400` -> `text-on-surface-variant`
- NOTE: Do NOT apply backdrop-blur to this toast per Pitfall 13 -- it appears during 1Hz GSI updates and backdrop-blur causes frame drops. Use solid `bg-surface-container-highest` instead.

**ErrorBanner.tsx**:
- Error type container: `bg-amber-900/30 border border-amber-500/50 rounded-lg` -> `bg-primary-container/15 border border-outline-variant/15` REMOVE `rounded-lg`. Use crimson tonal surface instead of amber (DESIGN.md crimson palette).
- Error text: `text-amber-400` -> `text-primary` (#FFB4AC crimson)
- Dismiss button: `text-amber-400 hover:text-amber-300` -> `text-primary hover:text-primary/80`
- Fallback type container: `bg-amber-900/20 border border-amber-500/30 rounded-lg` -> `bg-secondary/10 border border-outline-variant/15` REMOVE `rounded-lg`. Use gold tonal surface for warnings (gold = caution in DESIGN.md palette).
- Fallback text: `text-amber-400/80` -> `text-secondary/80`
  </action>
  <verify>
    <automated>cd prismlab/frontend && grep -c "cyan-accent" src/components/toast/AutoRefreshToast.tsx src/components/timeline/ErrorBanner.tsx 2>/dev/null | grep -v ":0$" | wc -l && grep -c "rounded-lg" src/components/toast/AutoRefreshToast.tsx src/components/timeline/ErrorBanner.tsx 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero instances of `cyan-accent` in both files
    - Zero instances of `rounded-lg` in both files
    - AutoRefreshToast.tsx: uses `bg-surface-container-highest shadow-glow`, NO `backdrop-blur` (Pitfall 13)
    - AutoRefreshToast.tsx: icon and title use `text-secondary` (gold)
    - ErrorBanner.tsx: error uses `bg-primary-container/15 text-primary` (crimson)
    - ErrorBanner.tsx: fallback uses `bg-secondary/10 text-secondary/80` (gold warning)
    - npm run build succeeds
  </acceptance_criteria>
  <done>AutoRefreshToast uses surface-container-highest with crimson glow (no backdrop-blur for performance). ErrorBanner uses crimson tonal surface for errors and gold for fallback warnings. No rounded corners, no cyan accent references.</done>
</task>

</tasks>

<verification>
1. `cd prismlab/frontend && npm run build` succeeds
2. `grep -r "cyan-accent" src/components/settings/ src/components/screenshot/ src/components/toast/ src/components/timeline/ErrorBanner.tsx` returns zero
3. `grep "backdrop-blur" src/components/settings/SettingsPanel.tsx src/components/screenshot/ScreenshotParser.tsx` returns matches (blood-glass on modals)
4. `grep "backdrop-blur" src/components/toast/AutoRefreshToast.tsx` returns zero (no blur on toast per Pitfall 13)
5. `grep "shadow-glow" src/components/settings/SettingsPanel.tsx src/components/screenshot/ScreenshotParser.tsx` returns matches
</verification>

<success_criteria>
- Blood-glass backdrops on SettingsPanel and ScreenshotParser
- Ambient crimson glow on all floating elements
- Toast uses solid background (no backdrop-blur for performance)
- All inputs follow Sacrificial Table pattern
- No rounded corners on floating elements
- No cyan-accent references remain
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-migration/17-04-SUMMARY.md`
</output>
