---
phase: 04-item-timeline-and-end-to-end-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/frontend/src/types/recommendation.ts
  - prismlab/frontend/src/api/client.ts
  - prismlab/frontend/src/stores/recommendationStore.ts
  - prismlab/frontend/src/hooks/useRecommendation.ts
  - prismlab/frontend/src/components/draft/GetBuildButton.tsx
autonomous: true
requirements: [DRFT-08, DISP-05]

must_haves:
  truths:
    - "GetBuildButton click fires POST /api/recommend with full draft state from gameStore"
    - "Recommendation response data is stored in recommendationStore and accessible to any component"
    - "Loading state is true during API call and false after response or error"
    - "Error state captures failure message when API call fails"
    - "Fallback flag from backend response is preserved and accessible"
  artifacts:
    - path: "prismlab/frontend/src/types/recommendation.ts"
      provides: "TypeScript types matching backend RecommendResponse, RecommendPhase, ItemRecommendation schemas"
      exports: ["ItemRecommendation", "RecommendPhase", "RecommendResponse"]
    - path: "prismlab/frontend/src/api/client.ts"
      provides: "postJson utility and api.recommend() method"
      exports: ["api"]
    - path: "prismlab/frontend/src/stores/recommendationStore.ts"
      provides: "Zustand store for recommendation state: data, loading, error, selectedItem"
      exports: ["useRecommendationStore"]
    - path: "prismlab/frontend/src/hooks/useRecommendation.ts"
      provides: "Hook wrapping store + API call logic"
      exports: ["useRecommendation"]
    - path: "prismlab/frontend/src/components/draft/GetBuildButton.tsx"
      provides: "CTA button wired to fire recommend() on click"
  key_links:
    - from: "prismlab/frontend/src/hooks/useRecommendation.ts"
      to: "/api/recommend"
      via: "api.recommend() POST call"
      pattern: "api\\.recommend"
    - from: "prismlab/frontend/src/hooks/useRecommendation.ts"
      to: "prismlab/frontend/src/stores/recommendationStore.ts"
      via: "sets data/loading/error in store"
      pattern: "useRecommendationStore"
    - from: "prismlab/frontend/src/components/draft/GetBuildButton.tsx"
      to: "prismlab/frontend/src/hooks/useRecommendation.ts"
      via: "calls recommend() from hook"
      pattern: "useRecommendation"
---

<objective>
Create the data plumbing for the recommendation flow: TypeScript types matching backend schemas, API client extension, Zustand recommendation store, useRecommendation hook, and GetBuildButton wiring.

Purpose: Establishes the complete data path from user click to API call to stored response. Plan 02 consumes this to render the timeline UI.
Output: Working API call triggered by GetBuildButton, response stored in recommendationStore, loading/error states tracked.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-item-timeline-and-end-to-end-flow/04-CONTEXT.md

<interfaces>
<!-- Backend schemas the frontend types MUST match exactly -->

From prismlab/backend/engine/schemas.py:

class ItemRecommendation(BaseModel):
    item_id: int
    item_name: str
    reasoning: str
    priority: str  # "core" | "situational" | "luxury"
    conditions: str | None = None  # For situational decision tree cards

class RecommendPhase(BaseModel):
    phase: str  # "starting" | "laning" | "core" | "late_game" | "situational"
    items: list[ItemRecommendation]
    timing: str | None = None
    gold_budget: int | None = None

class RecommendResponse(BaseModel):
    phases: list[RecommendPhase]
    overall_strategy: str | None = None
    fallback: bool = False
    model: str | None = None
    latency_ms: int | None = None

class RecommendRequest(BaseModel):
    hero_id: int
    role: int = Field(ge=1, le=5)
    playstyle: str
    side: str  # "radiant" | "dire"
    lane: str  # "safe" | "off" | "mid"
    lane_opponents: list[int] = Field(default_factory=list, max_length=2)
    allies: list[int] = Field(default_factory=list, max_length=4)

From prismlab/frontend/src/api/client.ts:

const API_BASE = "/api";
async function fetchJson<T>(url: string): Promise<T> { ... }
export const api = {
  getHeroes: () => fetchJson<Hero[]>("/heroes"),
  ...
};

From prismlab/frontend/src/stores/gameStore.ts:

interface GameStore {
  selectedHero: Hero | null;
  allies: (Hero | null)[];
  opponents: (Hero | null)[];
  role: number | null;
  playstyle: string | null;
  side: "radiant" | "dire" | null;
  lane: "safe" | "mid" | "off" | null;
  laneOpponents: Hero[];
}
export const useGameStore = create<GameStore>()((set, get) => ({ ... }));

From prismlab/frontend/src/components/draft/GetBuildButton.tsx:
Currently onClick logs to console -- needs wiring to useRecommendation.recommend()
isReady = selectedHero !== null && role !== null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recommendation types, extend API client, and create recommendationStore</name>
  <read_first>
    - prismlab/backend/engine/schemas.py
    - prismlab/frontend/src/api/client.ts
    - prismlab/frontend/src/stores/gameStore.ts
    - prismlab/frontend/src/types/item.ts
    - prismlab/frontend/src/types/hero.ts
  </read_first>
  <files>
    prismlab/frontend/src/types/recommendation.ts,
    prismlab/frontend/src/api/client.ts,
    prismlab/frontend/src/stores/recommendationStore.ts
  </files>
  <action>
    1. Create prismlab/frontend/src/types/recommendation.ts with TypeScript interfaces matching backend Pydantic schemas EXACTLY:
       - ItemRecommendation: { item_id: number; item_name: string; reasoning: string; priority: "core" | "situational" | "luxury"; conditions: string | null; }
       - RecommendPhase: { phase: "starting" | "laning" | "core" | "late_game" | "situational"; items: ItemRecommendation[]; timing: string | null; gold_budget: number | null; }
       - RecommendResponse: { phases: RecommendPhase[]; overall_strategy: string | null; fallback: boolean; model: string | null; latency_ms: number | null; }
       - RecommendRequest: { hero_id: number; role: number; playstyle: string; side: "radiant" | "dire"; lane: "safe" | "off" | "mid"; lane_opponents: number[]; allies: number[]; }

    2. Extend prismlab/frontend/src/api/client.ts:
       - Add import for RecommendRequest and RecommendResponse from types/recommendation
       - Add a generic postJson<TReq, TRes>(url: string, body: TReq): Promise<TRes> function alongside existing fetchJson. It should POST to ${API_BASE}${url} with Content-Type application/json, throw on non-ok response (include response status and statusText in error message), and return parsed JSON.
       - Add recommend: (req: RecommendRequest) => postJson<RecommendRequest, RecommendResponse>("/recommend", req) to the api object.

    3. Create prismlab/frontend/src/stores/recommendationStore.ts as a Zustand store following the same create() pattern as gameStore:
       - State: data: RecommendResponse | null, isLoading: boolean (default false), error: string | null, selectedItemId: string | null (composite key "phase-itemId" for which item reasoning is expanded)
       - Actions:
         - setData(data: RecommendResponse) -- sets data, clears error, sets isLoading false
         - setLoading(loading: boolean) -- sets isLoading
         - setError(error: string) -- sets error, clears data, sets isLoading false
         - selectItem(phaseItemKey: string | null) -- sets selectedItemId (toggle: if same key, set null)
         - clear() -- resets all state to defaults
       - Export as useRecommendationStore
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "ItemRecommendation" prismlab/frontend/src/types/recommendation.ts returns interface with item_id, item_name, reasoning, priority, conditions fields
    - grep "RecommendResponse" prismlab/frontend/src/types/recommendation.ts returns interface with phases, overall_strategy, fallback, model, latency_ms fields
    - grep "postJson" prismlab/frontend/src/api/client.ts returns the POST utility function
    - grep "recommend:" prismlab/frontend/src/api/client.ts returns api.recommend method
    - grep "useRecommendationStore" prismlab/frontend/src/stores/recommendationStore.ts returns the exported store
    - grep "isLoading" prismlab/frontend/src/stores/recommendationStore.ts returns loading state field
    - grep "selectedItemId" prismlab/frontend/src/stores/recommendationStore.ts returns the selected item tracking field
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - recommendation.ts exports all 4 interfaces matching backend schemas exactly (field names, types, nullability)
    - api/client.ts has postJson and api.recommend() that POSTs RecommendRequest and returns RecommendResponse
    - recommendationStore has data/isLoading/error/selectedItemId state with setData/setLoading/setError/selectItem/clear actions
    - All existing code still compiles (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useRecommendation hook and wire GetBuildButton</name>
  <read_first>
    - prismlab/frontend/src/stores/recommendationStore.ts
    - prismlab/frontend/src/stores/gameStore.ts
    - prismlab/frontend/src/api/client.ts
    - prismlab/frontend/src/types/recommendation.ts
    - prismlab/frontend/src/components/draft/GetBuildButton.tsx
  </read_first>
  <files>
    prismlab/frontend/src/hooks/useRecommendation.ts,
    prismlab/frontend/src/components/draft/GetBuildButton.tsx
  </files>
  <action>
    1. Create prismlab/frontend/src/hooks/useRecommendation.ts:
       - Import useRecommendationStore and useGameStore and api
       - Export function useRecommendation() that returns: { recommend, data, isLoading, error, selectedItemId, selectItem, clear }
       - recommend is an async function that:
         a. Reads current gameStore state via useGameStore.getState() (not hook -- direct store access for non-reactive read)
         b. Validates selectedHero and role are non-null (early return if not -- should not happen since button is disabled)
         c. Calls store.clear() to reset previous results, then store.setLoading(true) to show loading
         d. Builds a RecommendRequest from gameStore state:
            - hero_id: selectedHero.id
            - role: role (number)
            - playstyle: playstyle ?? "balanced" (fallback default)
            - side: side ?? "radiant" (fallback default)
            - lane: lane ?? "safe" (fallback default)
            - lane_opponents: laneOpponents.map(h => h.id)
            - allies: allies.filter(Boolean).map(h => h!.id)
         e. Wraps api.recommend(request) in try/catch
         f. On success: calls store.setData(response)
         g. On error: calls store.setError(err instanceof Error ? err.message : "Failed to get recommendations")
       - data, isLoading, error, selectedItemId, selectItem, clear are read from useRecommendationStore via individual selectors (for render optimization)

    2. Update prismlab/frontend/src/components/draft/GetBuildButton.tsx:
       - Import useRecommendation hook
       - Get recommend and isLoading from useRecommendation()
       - Replace console.log in onClick with recommend()
       - When isLoading is true, show "Analyzing..." text and add a subtle pulse/opacity animation to the button, also disable the button
       - Keep existing disabled logic (isReady = hero + role selected) but also disable when isLoading
       - Button text: isLoading ? "Analyzing..." : "Get Item Build"
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useRecommendation" prismlab/frontend/src/hooks/useRecommendation.ts returns the exported hook function
    - grep "api.recommend" prismlab/frontend/src/hooks/useRecommendation.ts returns the API call
    - grep "useGameStore.getState" prismlab/frontend/src/hooks/useRecommendation.ts returns direct store access for request building
    - grep "setLoading" prismlab/frontend/src/hooks/useRecommendation.ts returns loading state management
    - grep "setError" prismlab/frontend/src/hooks/useRecommendation.ts returns error handling
    - grep "useRecommendation" prismlab/frontend/src/components/draft/GetBuildButton.tsx returns hook import/usage
    - grep "Analyzing" prismlab/frontend/src/components/draft/GetBuildButton.tsx returns loading state button text
    - grep "recommend()" prismlab/frontend/src/components/draft/GetBuildButton.tsx returns the onClick wiring
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - useRecommendation hook reads gameStore, builds RecommendRequest, calls api.recommend(), stores result in recommendationStore
    - GetBuildButton calls recommend() on click instead of console.log
    - Button shows "Analyzing..." and disables during loading
    - Error from API call is captured in recommendationStore.error
    - All existing code still compiles
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: cd prismlab/frontend && npx tsc --noEmit
- GetBuildButton imports useRecommendation and calls recommend()
- recommendationStore exports useRecommendationStore with data/isLoading/error/selectedItemId
- api.recommend() POSTs to /api/recommend
- Types in recommendation.ts match backend schemas (field names, types, nullability)
</verification>

<success_criteria>
- Clicking "Get Item Build" with hero+role selected fires POST /api/recommend with full draft state
- Loading state is true during API call, button shows "Analyzing..."
- Successful response stored in recommendationStore.data
- Failed response stored in recommendationStore.error
- All TypeScript compilation passes
</success_criteria>

<output>
After completion, create .planning/phases/04-item-timeline-and-end-to-end-flow/04-01-SUMMARY.md
</output>