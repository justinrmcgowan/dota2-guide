---
phase: 04-item-timeline-and-end-to-end-flow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx
  - prismlab/frontend/src/components/timeline/ErrorBanner.tsx
  - prismlab/frontend/src/components/timeline/ItemCard.tsx
  - prismlab/frontend/src/components/timeline/PhaseCard.tsx
  - prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx
  - prismlab/frontend/src/components/timeline/ItemTimeline.tsx
  - prismlab/frontend/src/components/layout/MainPanel.tsx
autonomous: false
requirements: [DISP-01, DISP-02, DISP-03, DISP-04, DISP-05]

must_haves:
  truths:
    - "Item timeline renders in distinct phases: starting, laning, core, late game"
    - "Each item shows a Steam CDN portrait at 48px with gold cost below"
    - "Clicking an item expands 1-3 sentence reasoning panel below the phase card"
    - "Situational items display as branching decision tree cards with condition text and item options"
    - "Loading skeleton with pulsing placeholders appears during API call"
    - "Error displays as amber alert bar at top of MainPanel with dismiss button"
    - "Fallback notice appears as subtle amber banner when AI reasoning is unavailable"
  artifacts:
    - path: "prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx"
      provides: "Pulsing skeleton matching timeline layout structure"
    - path: "prismlab/frontend/src/components/timeline/ErrorBanner.tsx"
      provides: "Amber alert bar with dismiss button for errors, subtle banner for fallback"
    - path: "prismlab/frontend/src/components/timeline/ItemCard.tsx"
      provides: "Single item with Steam CDN portrait, gold cost, click to select"
    - path: "prismlab/frontend/src/components/timeline/PhaseCard.tsx"
      provides: "Phase header with horizontal row of ItemCards and expandable reasoning panel"
    - path: "prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx"
      provides: "Branching card with condition text and 2-3 item options"
    - path: "prismlab/frontend/src/components/timeline/ItemTimeline.tsx"
      provides: "Vertical stack of PhaseCards rendering full recommendation response"
    - path: "prismlab/frontend/src/components/layout/MainPanel.tsx"
      provides: "Main panel rendering timeline, skeleton, error, or empty state based on store"
  key_links:
    - from: "prismlab/frontend/src/components/timeline/ItemTimeline.tsx"
      to: "prismlab/frontend/src/stores/recommendationStore.ts"
      via: "useRecommendationStore selectors for data and selectedItemId"
      pattern: "useRecommendationStore"
    - from: "prismlab/frontend/src/components/timeline/ItemCard.tsx"
      to: "prismlab/frontend/src/utils/imageUrls.ts"
      via: "itemImageUrl for Steam CDN portraits"
      pattern: "itemImageUrl"
    - from: "prismlab/frontend/src/components/layout/MainPanel.tsx"
      to: "prismlab/frontend/src/components/timeline/ItemTimeline.tsx"
      via: "renders ItemTimeline when data exists"
      pattern: "ItemTimeline"
---

<objective>
Build the item timeline UI components and integrate them into MainPanel, rendering phased item recommendations with reasoning, loading skeletons, error banners, and situational decision trees.

Purpose: Completes the visual loop -- users see their recommendations rendered as a polished phased timeline with analytical reasoning.
Output: Full end-to-end flow working: draft inputs -> Get Item Build -> loading skeleton -> item timeline with reasoning.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-item-timeline-and-end-to-end-flow/04-CONTEXT.md
@.planning/phases/04-item-timeline-and-end-to-end-flow/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- types and store the UI components consume -->

From prismlab/frontend/src/types/recommendation.ts:

export interface ItemRecommendation {
  item_id: number;
  item_name: string;
  reasoning: string;
  priority: "core" | "situational" | "luxury";
  conditions: string | null;
}

export interface RecommendPhase {
  phase: "starting" | "laning" | "core" | "late_game" | "situational";
  items: ItemRecommendation[];
  timing: string | null;
  gold_budget: number | null;
}

export interface RecommendResponse {
  phases: RecommendPhase[];
  overall_strategy: string | null;
  fallback: boolean;
  model: string | null;
  latency_ms: number | null;
}

From prismlab/frontend/src/stores/recommendationStore.ts:

export const useRecommendationStore = create<RecommendationStore>()((set, get) => ({
  data: null,           // RecommendResponse | null
  isLoading: false,     // boolean
  error: null,          // string | null
  selectedItemId: null, // string | null ("phase-itemId" composite key)
  setData, setLoading, setError, selectItem, clear
}));

From prismlab/frontend/src/hooks/useRecommendation.ts:

export function useRecommendation(): {
  recommend: () => Promise<void>;
  data: RecommendResponse | null;
  isLoading: boolean;
  error: string | null;
  selectedItemId: string | null;
  selectItem: (key: string | null) => void;
  clear: () => void;
}

From prismlab/frontend/src/utils/imageUrls.ts:

export const itemImageUrl = (itemSlug: string) =>
  "https://cdn.cloudflare.steamstatic.com/apps/dota2/images/dota_react/items/${itemSlug}.png";

From prismlab/frontend/src/components/layout/MainPanel.tsx:
Currently shows selected hero info and placeholder text. Will be replaced with timeline/skeleton/error rendering.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timeline components (LoadingSkeleton, ErrorBanner, ItemCard, PhaseCard, DecisionTreeCard, ItemTimeline)</name>
  <read_first>
    - prismlab/frontend/src/types/recommendation.ts
    - prismlab/frontend/src/stores/recommendationStore.ts
    - prismlab/frontend/src/utils/imageUrls.ts
    - prismlab/frontend/src/types/item.ts
    - prismlab/frontend/src/components/draft/HeroPortrait.tsx
    - prismlab/frontend/src/components/layout/MainPanel.tsx
    - prismlab/frontend/src/hooks/useRecommendation.ts
  </read_first>
  <files>
    prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx,
    prismlab/frontend/src/components/timeline/ErrorBanner.tsx,
    prismlab/frontend/src/components/timeline/ItemCard.tsx,
    prismlab/frontend/src/components/timeline/PhaseCard.tsx,
    prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx,
    prismlab/frontend/src/components/timeline/ItemTimeline.tsx
  </files>
  <action>
    Create the prismlab/frontend/src/components/timeline/ directory and all 6 components:

    1. LoadingSkeleton.tsx:
       - Renders 4 skeleton phase cards (Starting, Laning, Core, Late Game) matching the timeline layout
       - Each skeleton card has: a pulsing header bar (h-6 w-24 bg-bg-elevated rounded animate-pulse), and a row of 3-4 pulsing item placeholders (h-12 w-12 bg-bg-elevated rounded animate-pulse) with pulsing text below each (h-3 w-10)
       - Use Tailwind animate-pulse class on bg-bg-elevated backgrounds
       - Overall vertical stack with gap-4 matching real timeline spacing

    2. ErrorBanner.tsx:
       - Props: { message: string; onDismiss: () => void; type: "error" | "fallback" }
       - For type="error": amber/yellow background (bg-amber-900/30 border border-amber-500/50), amber text, dismiss X button on right, message on left. Full width bar.
       - For type="fallback": subtle amber banner (bg-amber-900/20 border border-amber-500/30), smaller text, text: "AI reasoning unavailable -- showing basic recommendations". No dismiss button needed.
       - Both use rounded-lg and px-4 py-3 for padding

    3. ItemCard.tsx:
       - Props: { item: ItemRecommendation; phaseKey: string; isSelected: boolean; onSelect: () => void }
       - Renders item portrait from Steam CDN using itemImageUrl(item.item_name) at 48x48px (w-12 h-12)
       - Gold cost displayed below the image in small amber/gold text (text-amber-400 text-xs)
       - The item_name from the backend uses internal names (e.g., "blink" for Blink Dagger). Use itemImageUrl directly with item_name.
       - Priority indicator: thin left border on the card -- cyan for "core", amber for "situational", purple for "luxury"
       - When isSelected: highlight with cyan border/glow (ring-1 ring-cyan-accent)
       - onClick calls onSelect
       - Cursor pointer, transition-all for hover effects
       - Show item_name as tooltip (title attribute), formatted: replace underscores with spaces, title case

    4. PhaseCard.tsx:
       - Props: { phase: RecommendPhase; selectedItemId: string | null; onSelectItem: (key: string | null) => void }
       - Header bar: phase name displayed in uppercase with phase-specific color accent
         - "starting" -> gray-400
         - "laning" -> cyan-accent (#00d4ff)
         - "core" -> amber-400
         - "late_game" -> purple-400 (display as "LATE GAME")
         - "situational" -> amber-400
       - Timing and gold_budget shown in header if present (text-xs text-gray-500)
       - Items rendered as horizontal flex row (flex-wrap gap-3) of ItemCard components
       - Separate core/luxury items from situational items (items with conditions != null)
       - Render situational items via DecisionTreeCard below the main item row
       - Expandable reasoning panel: when an item is selected (selectedItemId matches "phase-itemId"), show a reasoning panel below the items row with the item reasoning text
         - Panel styling: bg-bg-elevated/50 rounded-lg p-4 mt-3 border-l-2 border-cyan-accent
         - Text: text-gray-300 text-sm leading-relaxed
         - Item name as small cyan label above the reasoning text
       - Compute composite key for each item as: phase.phase + "-" + item.item_id
       - Card container: bg-bg-secondary rounded-lg p-4 border border-bg-elevated

    5. DecisionTreeCard.tsx:
       - Props: { items: ItemRecommendation[] } (items that have conditions != null)
       - Renders a branching card with header "Situational Options" in amber text
       - Each item rendered as a branch: condition text on the left (from item.conditions), arrow, then item icon (32px) + item name on the right
       - Branches stacked vertically with divider lines between them
       - Container: bg-bg-elevated/30 rounded-lg p-3 border border-amber-500/20
       - Each branch: flex row with items-center, gap-2

    6. ItemTimeline.tsx:
       - Props: { data: RecommendResponse; selectedItemId: string | null; onSelectItem: (key: string | null) => void }
       - Renders overall_strategy as a summary paragraph at the top if present (text-gray-400 text-sm italic mb-4, with a small label "Strategy" in cyan above it)
       - Maps over data.phases and renders a PhaseCard for each
       - Vertical stack with gap-4
       - Phase order follows the response order (backend already sorts: starting, laning, core, late_game, situational)
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "animate-pulse" prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx returns pulsing skeleton elements
    - grep "onDismiss" prismlab/frontend/src/components/timeline/ErrorBanner.tsx returns dismiss handler prop
    - grep "itemImageUrl" prismlab/frontend/src/components/timeline/ItemCard.tsx returns Steam CDN image usage
    - grep "w-12 h-12" prismlab/frontend/src/components/timeline/ItemCard.tsx returns 48px item portrait
    - grep "text-amber-400" prismlab/frontend/src/components/timeline/ItemCard.tsx returns gold cost styling
    - grep "reasoning" prismlab/frontend/src/components/timeline/PhaseCard.tsx returns reasoning panel rendering
    - grep "selectedItemId" prismlab/frontend/src/components/timeline/PhaseCard.tsx returns item selection handling
    - grep "conditions" prismlab/frontend/src/components/timeline/DecisionTreeCard.tsx returns condition text rendering
    - grep "PhaseCard" prismlab/frontend/src/components/timeline/ItemTimeline.tsx returns phase card rendering
    - grep "overall_strategy" prismlab/frontend/src/components/timeline/ItemTimeline.tsx returns strategy display
    - All 6 files exist in prismlab/frontend/src/components/timeline/
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - LoadingSkeleton renders 4 pulsing phase card skeletons matching timeline layout
    - ErrorBanner renders amber alert bar (error) or subtle fallback banner
    - ItemCard renders 48px Steam CDN portrait with gold cost and priority border
    - PhaseCard renders phase header, item row, situational decision tree, and expandable reasoning panel
    - DecisionTreeCard renders branching conditions with item icons
    - ItemTimeline renders vertical stack of PhaseCards with optional strategy summary
    - All components compile and accept correct props
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate timeline into MainPanel with loading, error, and fallback states</name>
  <read_first>
    - prismlab/frontend/src/components/layout/MainPanel.tsx
    - prismlab/frontend/src/hooks/useRecommendation.ts
    - prismlab/frontend/src/stores/recommendationStore.ts
    - prismlab/frontend/src/stores/gameStore.ts
    - prismlab/frontend/src/components/timeline/ItemTimeline.tsx
    - prismlab/frontend/src/components/timeline/LoadingSkeleton.tsx
    - prismlab/frontend/src/components/timeline/ErrorBanner.tsx
  </read_first>
  <files>
    prismlab/frontend/src/components/layout/MainPanel.tsx
  </files>
  <action>
    Rewrite MainPanel.tsx to be the central hub that renders the right content based on recommendation state:

    1. Import useRecommendation hook, ItemTimeline, LoadingSkeleton, ErrorBanner, and useGameStore
    2. Get data, isLoading, error, selectedItemId, selectItem, clear from useRecommendation()
    3. Get selectedHero from useGameStore

    4. Render logic (in priority order):
       a. If error is non-null: render ErrorBanner with type="error", message=error, onDismiss={() => clear()} at the TOP of the panel. If data also exists (error after partial success), still show timeline below.
       b. If isLoading: render LoadingSkeleton
       c. If data exists:
          - If data.fallback is true: render ErrorBanner with type="fallback" at top
          - Render ItemTimeline with data, selectedItemId, onSelectItem=selectItem
       d. If no data and no loading and no error:
          - If selectedHero exists: show prompt text "Configure your draft and click Get Item Build to see recommendations" centered in the panel (text-gray-500)
          - If no selectedHero: show "Select a hero from the sidebar to get started" centered (existing behavior)

    5. MainPanel container: keep existing main tag with flex-1 bg-bg-primary overflow-y-auto p-6
    6. When hero is selected and data exists, show hero name + portrait at top above timeline (keep the existing hero display but make it more compact -- just the hero icon 32px + name inline, not the large portrait)
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "ItemTimeline" prismlab/frontend/src/components/layout/MainPanel.tsx returns timeline component usage
    - grep "LoadingSkeleton" prismlab/frontend/src/components/layout/MainPanel.tsx returns skeleton during loading
    - grep "ErrorBanner" prismlab/frontend/src/components/layout/MainPanel.tsx returns error/fallback banner usage
    - grep "useRecommendation" prismlab/frontend/src/components/layout/MainPanel.tsx returns hook usage
    - grep "isLoading" prismlab/frontend/src/components/layout/MainPanel.tsx returns loading state check
    - grep "fallback" prismlab/frontend/src/components/layout/MainPanel.tsx returns fallback banner rendering
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - MainPanel renders LoadingSkeleton during API call
    - MainPanel renders ErrorBanner (amber) when error occurs, with dismiss
    - MainPanel renders fallback banner when data.fallback is true
    - MainPanel renders ItemTimeline when data is available
    - MainPanel shows appropriate empty states when no data/hero
    - Full end-to-end flow works: sidebar inputs -> Get Item Build -> loading -> timeline
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of end-to-end flow</name>
  <files>None (visual verification only)</files>
  <action>
    Human verifies the complete end-to-end recommendation flow visually:
    Complete end-to-end recommendation flow: draft inputs in sidebar, Get Item Build button triggers API call, loading skeleton displays during API call, item timeline renders with phased cards showing item portraits from Steam CDN with gold costs, click an item to expand reasoning panel, error/fallback banners display appropriately.

    Verification steps:
    1. Start both containers: cd prismlab && docker compose up --build
    2. Visit http://localhost:8421
    3. Select a hero (e.g., Anti-Mage), pick Pos 1 role
    4. Optionally fill in opponents, lane opponents, side, lane
    5. Click "Get Item Build" -- verify button shows "Analyzing..." with subtle animation
    6. Verify loading skeleton appears in main panel with pulsing placeholders
    7. After 2-10 seconds, verify item timeline renders with phase cards (Starting, Laning, Core, Late Game)
    8. Verify each item shows a portrait image from Steam CDN at ~48px with gold cost below
    9. Click an item -- verify reasoning panel expands below the phase card with 1-3 sentences
    10. Click a different item in the same phase -- verify reasoning switches
    11. If there are situational items, verify branching decision tree card appears
    12. Test error handling: stop the backend container, click Get Item Build again -- verify amber error banner appears
    13. Verify dismiss button on error banner clears the error
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User confirms end-to-end flow works: draft -> recommend -> loading -> timeline with reasoning, errors handled</done>
</task>

</tasks>

<verification>
- cd prismlab/frontend && npx tsc --noEmit (TypeScript compiles)
- cd prismlab && docker compose up --build (containers start)
- Full flow: select hero + role -> Get Item Build -> loading skeleton -> item timeline with reasoning
- Error state: amber banner with dismiss
- Fallback state: subtle amber banner when AI unavailable
</verification>

<success_criteria>
- Item timeline displays recommendations in 4 phases with Steam CDN item portraits and gold costs
- Click-to-expand reasoning shows 1-3 sentence analytical explanation per item
- Situational items render as decision tree cards with conditions
- Loading skeleton appears during 2-10 second API call
- Error and fallback banners display appropriately in amber
- Complete end-to-end loop works: draft -> recommend -> display
</success_criteria>

<output>
After completion, create .planning/phases/04-item-timeline-and-end-to-end-flow/04-02-SUMMARY.md
</output>