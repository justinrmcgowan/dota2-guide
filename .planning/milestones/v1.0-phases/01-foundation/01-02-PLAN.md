---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/frontend/Dockerfile
  - prismlab/frontend/nginx.conf
  - prismlab/frontend/package.json
  - prismlab/frontend/vite.config.ts
  - prismlab/frontend/vitest.config.ts
  - prismlab/frontend/tsconfig.json
  - prismlab/frontend/tsconfig.app.json
  - prismlab/frontend/tsconfig.node.json
  - prismlab/frontend/index.html
  - prismlab/frontend/public/favicon.svg
  - prismlab/frontend/src/main.tsx
  - prismlab/frontend/src/App.tsx
  - prismlab/frontend/src/vite-env.d.ts
  - prismlab/frontend/src/styles/globals.css
  - prismlab/frontend/src/types/hero.ts
  - prismlab/frontend/src/types/item.ts
  - prismlab/frontend/src/utils/imageUrls.ts
  - prismlab/frontend/src/utils/constants.ts
  - prismlab/frontend/src/stores/gameStore.ts
  - prismlab/frontend/src/api/client.ts
  - prismlab/frontend/src/components/layout/Header.tsx
  - prismlab/frontend/src/components/layout/Sidebar.tsx
  - prismlab/frontend/src/components/layout/MainPanel.tsx
autonomous: true
requirements:
  - DISP-06

must_haves:
  truths:
    - "User sees a dark-themed interface with #0f1419 background and spectral cyan (#00d4ff) accent"
    - "A prism-shaped favicon appears in the browser tab"
    - "Layout shows a fixed 320px sidebar on the left and scrollable main panel on the right"
    - "Inter font is used for body text and JetBrains Mono for stats/numbers"
    - "Frontend builds and serves via Nginx in Docker on port 8421"
    - "Vitest test infrastructure is configured and ready for behavioral tests"
  artifacts:
    - path: "prismlab/frontend/src/styles/globals.css"
      provides: "Tailwind v4 theme with OKLCH colors"
      contains: "@theme"
    - path: "prismlab/frontend/public/favicon.svg"
      provides: "Prism/diamond SVG favicon"
      contains: "svg"
    - path: "prismlab/frontend/src/App.tsx"
      provides: "Root component with sidebar + main panel layout"
      contains: "Sidebar"
    - path: "prismlab/frontend/src/components/layout/Header.tsx"
      provides: "App header with Prismlab title"
      contains: "Prismlab"
    - path: "prismlab/frontend/src/components/layout/Sidebar.tsx"
      provides: "Fixed 320px sidebar shell"
      contains: "320"
    - path: "prismlab/frontend/src/components/layout/MainPanel.tsx"
      provides: "Scrollable main content area"
    - path: "prismlab/frontend/src/types/hero.ts"
      provides: "Hero TypeScript interface"
      contains: "interface Hero"
    - path: "prismlab/frontend/src/stores/gameStore.ts"
      provides: "Zustand store for hero selection"
      contains: "useGameStore"
    - path: "prismlab/frontend/nginx.conf"
      provides: "Nginx config with SPA routing and API proxy"
      contains: "proxy_pass"
    - path: "prismlab/frontend/Dockerfile"
      provides: "Multi-stage Docker build"
      contains: "nginx"
    - path: "prismlab/frontend/vitest.config.ts"
      provides: "Vitest configuration for React component and unit tests"
      contains: "jsdom"
  key_links:
    - from: "prismlab/frontend/src/styles/globals.css"
      to: "prismlab/frontend/src/main.tsx"
      via: "CSS import in entry point"
      pattern: "globals.css"
    - from: "prismlab/frontend/nginx.conf"
      to: "prismlab-backend"
      via: "reverse proxy for /api/"
      pattern: "proxy_pass.*prismlab-backend"
---

<objective>
Create the complete frontend shell: React + Vite + TypeScript + Tailwind v4 application with the dark theme, spectral cyan accent colors, layout structure (Header, Sidebar, MainPanel), favicon, type definitions, Zustand store, API client, Vitest test infrastructure, and Docker/Nginx configuration.

Purpose: This establishes the visual identity and frontend infrastructure. The hero picker (Plan 03) plugs into this shell.
Output: A dark-themed React application serving via Nginx in Docker on port 8421, with API proxy to backend.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@PRISMLAB_BLUEPRINT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: React + Vite + Tailwind v4 scaffolding with dark theme, layout, types, store, and test infrastructure</name>
  <files>
    prismlab/frontend/package.json,
    prismlab/frontend/vite.config.ts,
    prismlab/frontend/vitest.config.ts,
    prismlab/frontend/tsconfig.json,
    prismlab/frontend/tsconfig.app.json,
    prismlab/frontend/tsconfig.node.json,
    prismlab/frontend/index.html,
    prismlab/frontend/public/favicon.svg,
    prismlab/frontend/src/main.tsx,
    prismlab/frontend/src/App.tsx,
    prismlab/frontend/src/vite-env.d.ts,
    prismlab/frontend/src/styles/globals.css,
    prismlab/frontend/src/types/hero.ts,
    prismlab/frontend/src/types/item.ts,
    prismlab/frontend/src/utils/imageUrls.ts,
    prismlab/frontend/src/utils/constants.ts,
    prismlab/frontend/src/stores/gameStore.ts,
    prismlab/frontend/src/api/client.ts,
    prismlab/frontend/src/components/layout/Header.tsx,
    prismlab/frontend/src/components/layout/Sidebar.tsx,
    prismlab/frontend/src/components/layout/MainPanel.tsx
  </files>
  <read_first>
    PRISMLAB_BLUEPRINT.md (project structure, GameState interface, component hierarchy),
    .planning/phases/01-foundation/01-RESEARCH.md (Tailwind v4 CSS-first patterns, OKLCH color values, Zustand pattern, package versions, font setup),
    .planning/phases/01-foundation/01-CONTEXT.md (locked decisions: Inter font, JetBrains Mono for stats, #0f1419 background, 320px sidebar, SVG favicon with cyan gradient)
  </read_first>
  <action>
Scaffold the React frontend from scratch. Use `npm create vite@latest` to bootstrap, then customize.

**Step 1: Scaffold with Vite**
Run: `cd prismlab && npm create vite@latest frontend -- --template react-swc-ts`
This creates the base project. Then install dependencies:
```bash
cd prismlab/frontend
npm install tailwindcss @tailwindcss/vite zustand fuse.js @fontsource/inter @fontsource/jetbrains-mono
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

**Step 2: Configure Vite** -- prismlab/frontend/vite.config.ts:
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:8420",
        changeOrigin: true,
      },
    },
  },
});
```
The proxy enables local dev without Docker. In production, Nginx handles the proxy.

**Step 2b: Configure Vitest** -- prismlab/frontend/vitest.config.ts:
```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react-swc";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: [],
    include: ["src/**/*.test.{ts,tsx}"],
  },
});
```
This configures Vitest with jsdom for React component testing. The `globals: true` setting makes `describe`, `it`, `expect` available without imports. Plan 03 will add the first behavioral test (heroSearch.test.ts).

Also add a `"test"` script to package.json:
```json
"scripts": {
  "dev": "vite",
  "build": "tsc -b && vite build",
  "lint": "eslint .",
  "preview": "vite preview",
  "test": "vitest run"
}
```

**Step 3: Tailwind v4 CSS theme** -- prismlab/frontend/src/styles/globals.css:
IMPORTANT: Tailwind v4 uses CSS-first config. NO tailwind.config.js file. NO @tailwind directives.
```css
@import "tailwindcss";

@theme {
  --color-cyan-accent: oklch(80.4% 0.146 219.5);
  --color-radiant: oklch(89.5% 0.192 150.6);
  --color-dire: oklch(68.2% 0.206 24.4);
  --color-bg-primary: oklch(18.8% 0.013 248.5);
  --color-bg-secondary: oklch(23.3% 0.022 272.8);
  --color-bg-elevated: oklch(27% 0.02 260);

  --color-attr-str: oklch(68% 0.19 25);
  --color-attr-agi: oklch(75% 0.18 145);
  --color-attr-int: oklch(70% 0.15 250);
  --color-attr-all: oklch(80% 0.1 90);

  --font-body: "Inter", ui-sans-serif, system-ui, sans-serif;
  --font-stats: "JetBrains Mono", ui-monospace, monospace;
}
```
Then use in markup: bg-bg-primary, text-cyan-accent, font-body, font-stats.

**Step 4: Font imports** -- prismlab/frontend/src/main.tsx:
```typescript
import React from "react";
import ReactDOM from "react-dom/client";
import "@fontsource/inter/400.css";
import "@fontsource/inter/500.css";
import "@fontsource/inter/600.css";
import "@fontsource/inter/700.css";
import "@fontsource/jetbrains-mono/400.css";
import "@fontsource/jetbrains-mono/500.css";
import "./styles/globals.css";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**Step 5: Favicon** -- prismlab/frontend/public/favicon.svg:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <defs>
    <linearGradient id="prism" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#00d4ff" />
      <stop offset="50%" stop-color="#6aff97" />
      <stop offset="100%" stop-color="#00d4ff" />
    </linearGradient>
  </defs>
  <path d="M16 2 L28 26 L4 26 Z" fill="none" stroke="url(#prism)" stroke-width="2" />
  <path d="M16 8 L22 22 L10 22 Z" fill="url(#prism)" opacity="0.3" />
</svg>
```

**Step 6: index.html** -- update the Vite-generated index.html:
- Set `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`
- Set `<title>Prismlab</title>`
- Keep the `<div id="root"></div>` and `<script type="module" src="/src/main.tsx"></script>`

**Step 7: TypeScript types:**

prismlab/frontend/src/types/hero.ts:
```typescript
export interface Hero {
  id: number;
  name: string;
  localized_name: string;
  internal_name: string;
  primary_attr: "str" | "agi" | "int" | "all";
  attack_type: "Melee" | "Ranged";
  roles: string[];
  base_health: number;
  base_mana: number;
  base_armor: number;
  base_attack_min: number;
  base_attack_max: number;
  base_str: number;
  base_agi: number;
  base_int: number;
  str_gain: number;
  agi_gain: number;
  int_gain: number;
  attack_range: number;
  move_speed: number;
  img_url: string;
  icon_url: string;
}
```

prismlab/frontend/src/types/item.ts:
```typescript
export interface Item {
  id: number;
  name: string;
  internal_name: string;
  cost: number | null;
  components: string[] | null;
  is_recipe: boolean;
  is_neutral: boolean;
  tier: number | null;
  bonuses: Record<string, unknown>[] | null;
  active_desc: string | null;
  passive_desc: string | null;
  category: string | null;
  tags: string[] | null;
  img_url: string;
}
```

**Step 8: Utility files:**

prismlab/frontend/src/utils/imageUrls.ts:
```typescript
const HERO_CDN = "https://cdn.cloudflare.steamstatic.com/apps/dota2/images/dota_react/heroes";
const ITEM_CDN = "https://cdn.cloudflare.steamstatic.com/apps/dota2/images/dota_react/items";

export const heroImageUrl = (heroSlug: string) => `${HERO_CDN}/${heroSlug}.png`;
export const heroIconUrl = (heroSlug: string) => `${HERO_CDN}/icons/${heroSlug}.png`;
export const itemImageUrl = (itemSlug: string) => `${ITEM_CDN}/${itemSlug}.png`;

export const heroSlugFromInternal = (internalName: string) =>
  internalName.replace("npc_dota_hero_", "");
```

prismlab/frontend/src/utils/constants.ts:
```typescript
export const ATTR_COLORS: Record<string, string> = {
  str: "text-attr-str",
  agi: "text-attr-agi",
  int: "text-attr-int",
  all: "text-attr-all",
};

export const ATTR_BG_COLORS: Record<string, string> = {
  str: "bg-attr-str",
  agi: "bg-attr-agi",
  int: "bg-attr-int",
  all: "bg-attr-all",
};
```

**Step 9: Zustand store** -- prismlab/frontend/src/stores/gameStore.ts:
```typescript
import { create } from "zustand";
import type { Hero } from "../types/hero";

interface GameStore {
  selectedHero: Hero | null;
  selectHero: (hero: Hero) => void;
  clearHero: () => void;
}

export const useGameStore = create<GameStore>()((set) => ({
  selectedHero: null,
  selectHero: (hero) => set({ selectedHero: hero }),
  clearHero: () => set({ selectedHero: null }),
}));
```

**Step 10: API client** -- prismlab/frontend/src/api/client.ts:
```typescript
import type { Hero } from "../types/hero";
import type { Item } from "../types/item";

const API_BASE = "/api";

async function fetchJson<T>(url: string): Promise<T> {
  const response = await fetch(`${API_BASE}${url}`);
  if (!response.ok) {
    throw new Error(`API error: ${response.status} ${response.statusText}`);
  }
  return response.json();
}

export const api = {
  getHeroes: () => fetchJson<Hero[]>("/heroes"),
  getHero: (id: number) => fetchJson<Hero>(`/heroes/${id}`),
  getItems: () => fetchJson<Item[]>("/items"),
  getItem: (id: number) => fetchJson<Item>(`/items/${id}`),
};
```

**Step 11: Layout components:**

prismlab/frontend/src/components/layout/Header.tsx:
- Fixed header at top, full width, bg-bg-secondary, border-b border-bg-elevated
- Left: prism icon (inline SVG or text) + "Prismlab" in text-cyan-accent font-bold text-xl
- Height: h-14 with px-4 flex items-center
- Use font-body

prismlab/frontend/src/components/layout/Sidebar.tsx:
- Fixed width w-80 (320px), full height below header, bg-bg-secondary
- overflow-y-auto for scrolling when content exceeds viewport
- p-4 for inner padding
- Placeholder text: "Hero selection will go here" (replaced in Plan 03)

prismlab/frontend/src/components/layout/MainPanel.tsx:
- Takes remaining width (flex-1), full height below header
- overflow-y-auto for scrolling
- bg-bg-primary
- p-6 for inner padding
- Placeholder text: "Recommendations will appear here"

**Step 12: App.tsx** -- root layout:
```typescript
import Header from "./components/layout/Header";
import Sidebar from "./components/layout/Sidebar";
import MainPanel from "./components/layout/MainPanel";

function App() {
  return (
    <div className="h-screen flex flex-col bg-bg-primary text-gray-100 font-body">
      <Header />
      <div className="flex flex-1 overflow-hidden">
        <Sidebar />
        <MainPanel />
      </div>
    </div>
  );
}

export default App;
```

Delete any Vite-generated boilerplate files (App.css, index.css, logo.svg, etc.) that are not needed.
  </action>
  <verify>
    <automated>cd "C:/Users/Justin/Documents/Projects/Dota 2 Prismlab/prismlab/frontend" && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - prismlab/frontend/src/styles/globals.css contains "@import" and "@theme"
    - prismlab/frontend/src/styles/globals.css contains "oklch(80.4% 0.146 219.5)"
    - prismlab/frontend/src/styles/globals.css contains "--color-bg-primary"
    - prismlab/frontend/src/styles/globals.css contains "--font-body"
    - prismlab/frontend/src/styles/globals.css contains "--font-stats"
    - prismlab/frontend/public/favicon.svg contains "svg" and "prism"
    - prismlab/frontend/index.html contains "favicon.svg"
    - prismlab/frontend/index.html contains "Prismlab"
    - prismlab/frontend/src/main.tsx contains "@fontsource/inter"
    - prismlab/frontend/src/main.tsx contains "@fontsource/jetbrains-mono"
    - prismlab/frontend/src/main.tsx contains "globals.css"
    - prismlab/frontend/src/types/hero.ts contains "interface Hero"
    - prismlab/frontend/src/types/hero.ts contains "primary_attr"
    - prismlab/frontend/src/types/hero.ts contains "img_url"
    - prismlab/frontend/src/types/item.ts contains "interface Item"
    - prismlab/frontend/src/utils/imageUrls.ts contains "heroImageUrl"
    - prismlab/frontend/src/utils/imageUrls.ts contains "itemImageUrl"
    - prismlab/frontend/src/utils/imageUrls.ts contains "cdn.cloudflare.steamstatic.com"
    - prismlab/frontend/src/stores/gameStore.ts contains "useGameStore"
    - prismlab/frontend/src/stores/gameStore.ts contains "selectedHero"
    - prismlab/frontend/src/api/client.ts contains "getHeroes"
    - prismlab/frontend/src/api/client.ts contains "getItems"
    - prismlab/frontend/src/App.tsx contains "Sidebar"
    - prismlab/frontend/src/App.tsx contains "MainPanel"
    - prismlab/frontend/src/App.tsx contains "Header"
    - prismlab/frontend/src/App.tsx contains "bg-bg-primary"
    - prismlab/frontend/src/components/layout/Header.tsx contains "Prismlab"
    - prismlab/frontend/src/components/layout/Sidebar.tsx contains "w-80"
    - prismlab/frontend/vite.config.ts contains "tailwindcss"
    - prismlab/frontend/vite.config.ts contains "@tailwindcss/vite"
    - prismlab/frontend/vitest.config.ts contains "jsdom"
    - prismlab/frontend/vitest.config.ts contains "environment"
    - prismlab/frontend/package.json contains "vitest"
    - prismlab/frontend/package.json contains "@testing-library/react"
    - prismlab/frontend/package.json contains "@testing-library/jest-dom"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Frontend application scaffolded with Tailwind v4 dark theme, OKLCH color system, Inter + JetBrains Mono fonts, prism favicon, sidebar + main panel layout, TypeScript types for Hero and Item, Zustand store, API client, and Vitest test infrastructure (vitest, @testing-library/react, @testing-library/jest-dom, jsdom). TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Dockerfile, Nginx config, and build verification</name>
  <files>
    prismlab/frontend/Dockerfile,
    prismlab/frontend/nginx.conf
  </files>
  <read_first>
    prismlab/frontend/package.json (verify build script exists),
    prismlab/frontend/vite.config.ts (verify build output directory),
    .planning/phases/01-foundation/01-RESEARCH.md (Nginx reverse proxy pattern, multi-stage Dockerfile pattern)
  </read_first>
  <action>
**prismlab/frontend/Dockerfile** -- multi-stage build:
```dockerfile
FROM node:22-alpine AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine AS production
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**prismlab/frontend/nginx.conf** -- SPA routing + API reverse proxy:
```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing - all non-file requests go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to backend container
    location /api/ {
        proxy_pass http://prismlab-backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

This Nginx config does two things:
1. Serves the SPA with proper client-side routing (any non-file path serves index.html)
2. Proxies /api/ requests to the backend container (prismlab-backend:8000) so the frontend can call /api/heroes with a simple relative URL -- no CORS needed

After creating both files, verify the frontend builds successfully:
```bash
cd prismlab/frontend && npm run build
```
The build output should be in the `dist/` directory.
  </action>
  <verify>
    <automated>cd "C:/Users/Justin/Documents/Projects/Dota 2 Prismlab/prismlab/frontend" && npm run build && ls dist/index.html && echo "Build OK"</automated>
  </verify>
  <acceptance_criteria>
    - prismlab/frontend/Dockerfile contains "node:22-alpine"
    - prismlab/frontend/Dockerfile contains "npm run build"
    - prismlab/frontend/Dockerfile contains "nginx:alpine"
    - prismlab/frontend/Dockerfile contains "COPY --from=build /app/dist"
    - prismlab/frontend/nginx.conf contains "listen 80"
    - prismlab/frontend/nginx.conf contains "try_files $uri $uri/ /index.html"
    - prismlab/frontend/nginx.conf contains "proxy_pass http://prismlab-backend:8000/api/"
    - prismlab/frontend/nginx.conf contains "proxy_set_header Host"
    - npm run build exits with code 0
    - prismlab/frontend/dist/index.html exists after build
  </acceptance_criteria>
  <done>Frontend builds successfully via Vite. Dockerfile defines multi-stage build (node build + nginx serve). Nginx config routes SPA and proxies API requests to backend container. dist/index.html exists.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: cd prismlab/frontend && npx tsc --noEmit
2. Vite builds: cd prismlab/frontend && npm run build
3. dist/index.html exists with favicon reference
4. globals.css has all theme colors defined
5. Docker frontend builds: cd prismlab && docker compose build prismlab-frontend
6. Vitest config exists: test -f prismlab/frontend/vitest.config.ts
</verification>

<success_criteria>
- Dark-themed React app with #0f1419 background, spectral cyan accent (#00d4ff), Radiant teal, Dire red
- Inter font for body text, JetBrains Mono for stats
- Prism/diamond SVG favicon with cyan-to-teal gradient
- Fixed 320px sidebar + scrollable main panel layout
- TypeScript types for Hero and Item matching backend models
- Zustand store with hero selection state
- API client with typed fetch wrappers for /api/heroes and /api/items
- Nginx serves SPA and proxies /api/ to backend
- Frontend builds and dist/index.html exists
- Vitest configured with jsdom environment, @testing-library/react and @testing-library/jest-dom installed as devDependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
