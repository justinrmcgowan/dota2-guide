---
phase: 05-mid-game-adaptation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - prismlab/frontend/src/components/game/LaneResultSelector.tsx
  - prismlab/frontend/src/components/game/DamageProfileInput.tsx
  - prismlab/frontend/src/components/game/EnemyItemTracker.tsx
  - prismlab/frontend/src/components/game/GameStatePanel.tsx
  - prismlab/frontend/src/components/game/ReEvaluateButton.tsx
  - prismlab/frontend/src/components/layout/Sidebar.tsx
  - prismlab/frontend/src/utils/constants.ts
autonomous: true
requirements: [MIDG-02, MIDG-03, MIDG-04]

must_haves:
  truths:
    - "User can select lane result (Won/Even/Lost) via three-button toggle"
    - "User can input damage profile via quick toggles and fine-tune sliders"
    - "User can mark enemy items as spotted from a curated grid"
    - "User can hit Re-Evaluate and see updated recommendations"
    - "Game State section appears in sidebar only after first recommendation"
    - "Game State section is collapsible"
  artifacts:
    - path: "prismlab/frontend/src/components/game/LaneResultSelector.tsx"
      provides: "Won/Even/Lost three-button toggle"
      contains: "laneResult"
    - path: "prismlab/frontend/src/components/game/DamageProfileInput.tsx"
      provides: "Damage profile toggles and sliders"
      contains: "damageProfile"
    - path: "prismlab/frontend/src/components/game/EnemyItemTracker.tsx"
      provides: "Grid of ~15 counter items with toggle checkboxes"
      contains: "toggleEnemyItem"
    - path: "prismlab/frontend/src/components/game/GameStatePanel.tsx"
      provides: "Collapsible container for all mid-game inputs"
      contains: "GameStatePanel"
    - path: "prismlab/frontend/src/components/game/ReEvaluateButton.tsx"
      provides: "Cyan Re-Evaluate button triggering recommendation refresh"
      contains: "Re-Evaluate"
    - path: "prismlab/frontend/src/components/layout/Sidebar.tsx"
      provides: "GameStatePanel integrated after draft inputs"
      contains: "GameStatePanel"
  key_links:
    - from: "prismlab/frontend/src/components/game/ReEvaluateButton.tsx"
      to: "prismlab/frontend/src/hooks/useRecommendation.ts"
      via: "calls recommend() to trigger re-evaluation"
      pattern: "recommend"
    - from: "prismlab/frontend/src/components/game/LaneResultSelector.tsx"
      to: "prismlab/frontend/src/stores/gameStore.ts"
      via: "calls setLaneResult"
      pattern: "setLaneResult"
    - from: "prismlab/frontend/src/components/game/DamageProfileInput.tsx"
      to: "prismlab/frontend/src/stores/gameStore.ts"
      via: "calls setDamageProfile"
      pattern: "setDamageProfile"
    - from: "prismlab/frontend/src/components/game/EnemyItemTracker.tsx"
      to: "prismlab/frontend/src/stores/gameStore.ts"
      via: "calls toggleEnemyItem"
      pattern: "toggleEnemyItem"
    - from: "prismlab/frontend/src/components/layout/Sidebar.tsx"
      to: "prismlab/frontend/src/components/game/GameStatePanel.tsx"
      via: "renders GameStatePanel conditionally when recommendation data exists"
      pattern: "GameStatePanel"
---

<objective>
Build the mid-game adaptation UI: lane result selector, damage profile input, enemy item tracker, Re-Evaluate button, and the collapsible Game State panel in the sidebar.

Purpose: These components let the user feed live game information back to the recommendation engine. Combined with Plan 01 (data layer + purchased item tracking), this completes the "living advisor" loop where recommendations evolve as the game progresses.

Output: Five new game components, updated Sidebar with conditional Game State section, updated constants.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mid-game-adaptation/05-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01 that this plan consumes -->

From prismlab/frontend/src/stores/gameStore.ts (extended by Plan 01):
~~~typescript
// New mid-game state and actions added by Plan 01:
laneResult: "won" | "even" | "lost" | null;
damageProfile: { physical: number; magical: number; pure: number } | null;
enemyItemsSpotted: string[];
setLaneResult: (result: "won" | "even" | "lost") => void;
setDamageProfile: (profile: { physical: number; magical: number; pure: number }) => void;
toggleEnemyItem: (itemName: string) => void;
clearMidGameState: () => void;
~~~

From prismlab/frontend/src/hooks/useRecommendation.ts (extended by Plan 01):
~~~typescript
export function useRecommendation() {
  // recommend() now sends lane_result, damage_profile, enemy_items_spotted, purchased_items
  // clear() resets everything including purchased items (for starting fresh)
  return { recommend, data, isLoading, error, selectedItemId, selectItem, clear };
}
~~~

From prismlab/frontend/src/stores/recommendationStore.ts (extended by Plan 01):
~~~typescript
// New purchased item state:
purchasedItems: Set<string>;
togglePurchased: (phaseItemKey: string) => void;
getPurchasedItemIds: () => number[];
clearResults: () => void;  // clears data but keeps purchased items
clear: () => void;         // full reset
~~~

Existing component patterns to follow:

From prismlab/frontend/src/components/draft/RoleSelector.tsx:
~~~typescript
// Toggle button pattern: flex gap, button with aria-checked, conditional bg/text/border classes
// Active: bg-cyan-accent/20 text-cyan-accent border-cyan-accent
// Inactive: bg-bg-elevated text-gray-400 border-bg-elevated hover:text-gray-200
~~~

From prismlab/frontend/src/components/draft/GetBuildButton.tsx:
~~~typescript
// CTA button pattern: w-full py-3 rounded-lg font-semibold
// Active: bg-cyan-accent text-bg-primary hover:brightness-110 shadow glow
// Disabled: bg-bg-elevated text-gray-500 cursor-not-allowed
// Loading: animate-pulse opacity-80
~~~

From prismlab/frontend/src/components/layout/Sidebar.tsx:
~~~typescript
// Section label pattern: h2 text-sm font-semibold text-gray-400 uppercase tracking-wider mb-2 mt-5
// Layout: aside w-80 bg-bg-secondary, scrollable content area, pinned bottom button
~~~
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LaneResultSelector, DamageProfileInput, EnemyItemTracker, and constants</name>
  <read_first>
    - prismlab/frontend/src/components/draft/RoleSelector.tsx
    - prismlab/frontend/src/components/draft/SideSelector.tsx
    - prismlab/frontend/src/stores/gameStore.ts
    - prismlab/frontend/src/utils/constants.ts
    - prismlab/frontend/src/utils/imageUrls.ts
  </read_first>
  <files>
    prismlab/frontend/src/components/game/LaneResultSelector.tsx,
    prismlab/frontend/src/components/game/DamageProfileInput.tsx,
    prismlab/frontend/src/components/game/EnemyItemTracker.tsx,
    prismlab/frontend/src/utils/constants.ts
  </files>
  <action>
**constants.ts -- Add mid-game constants:**
Add these exports to the existing constants file:

LANE_RESULT_OPTIONS array: [
  { value: "won", label: "Won", color: "text-radiant" (teal) },
  { value: "even", label: "Even", color: "text-cyan-accent" },
  { value: "lost", label: "Lost", color: "text-dire" (red) }
]

DAMAGE_PRESETS array: [
  { label: "Heavy Physical", profile: { physical: 70, magical: 20, pure: 10 } },
  { label: "Heavy Magic", profile: { physical: 20, magical: 70, pure: 10 } },
  { label: "Mixed", profile: { physical: 40, magical: 40, pure: 20 } },
  { label: "Pure Burst", profile: { physical: 20, magical: 20, pure: 60 } }
]

ENEMY_COUNTER_ITEMS array of ~15 common counter items that change how you itemize (per Claude discretion). Each entry: { name: string (internal item name for API/store), label: string (display name) }. Include:
  bkb / Black King Bar, blink / Blink Dagger, force_staff / Force Staff,
  ghost / Ghost Scepter, sphere / Linkens Sphere, shivas_guard / Shivas Guard,
  blade_mail / Blade Mail, orchid / Orchid Malevolence, nullifier / Nullifier,
  heavens_halberd / Heavens Halberd, silver_edge / Silver Edge, aeon_disk / Aeon Disk,
  lotus_orb / Lotus Orb, assault / Assault Cuirass, pipe / Pipe of Insight

**LaneResultSelector.tsx -- Three-button toggle (same pattern as RoleSelector):**
- Import useGameStore, LANE_RESULT_OPTIONS from constants
- Read laneResult and setLaneResult from store
- Render a flex row of 3 buttons using LANE_RESULT_OPTIONS
- Active state: use the option color (Won = radiant teal bg-radiant/20, Even = cyan-accent bg-cyan-accent/20, Lost = dire red bg-dire/20) with matching border
- Inactive state: bg-bg-elevated text-gray-400 border-bg-elevated hover:text-gray-200
- Use role="radiogroup" and aria-checked pattern from RoleSelector

**DamageProfileInput.tsx -- Preset toggles + slider controls:**
- Import useGameStore, DAMAGE_PRESETS from constants
- Read damageProfile and setDamageProfile from store
- Render two sections:
  1. Quick preset row: flex-wrap gap-1.5 with small buttons for each DAMAGE_PRESETS entry. When clicked, call setDamageProfile with preset values. Highlight the active preset if current damageProfile matches exactly.
  2. Three range sliders below (Physical / Magical / Pure), each 0-100:
     - Use native HTML input type="range" with Tailwind styling
     - Show current value as a label next to the slider: "Physical: 60%"
     - When a slider changes, call setDamageProfile with updated values. The three should be independent (do NOT auto-normalize to 100 -- this matches how Dota damage is reported, sometimes exceeding or not summing to 100).
     - Slider accent color: Physical = gray-400, Magical = cyan-accent, Pure = purple-400
     - Style the range inputs with accent-color CSS property for the track/thumb
  3. If damageProfile is null (not yet set), show sliders at 0/0/0 with a subtle "Set damage from death screen" hint text

**EnemyItemTracker.tsx -- Grid of toggleable counter items:**
- Import useGameStore, ENEMY_COUNTER_ITEMS from constants, itemImageUrl from imageUrls
- Read enemyItemsSpotted and toggleEnemyItem from store
- Render a grid (grid-cols-5 gap-2) of item buttons
- Each button shows the item image (32x32, from Steam CDN via itemImageUrl) with the item label below in tiny text
- Active state (item spotted): ring-2 ring-dire (red ring indicating enemy has it), full opacity
- Inactive state: opacity-50 grayscale, hover:opacity-75 hover:grayscale-0
- On click: call toggleEnemyItem(item.name)
- Tooltip or title attribute with the full item name
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - prismlab/frontend/src/components/game/LaneResultSelector.tsx exists and exports default component
    - prismlab/frontend/src/components/game/DamageProfileInput.tsx exists and exports default component
    - prismlab/frontend/src/components/game/EnemyItemTracker.tsx exists and exports default component
    - grep -n "LANE_RESULT_OPTIONS" prismlab/frontend/src/utils/constants.ts returns a match
    - grep -n "DAMAGE_PRESETS" prismlab/frontend/src/utils/constants.ts returns a match
    - grep -n "ENEMY_COUNTER_ITEMS" prismlab/frontend/src/utils/constants.ts returns a match
    - npx tsc --noEmit exits cleanly with no errors
  </acceptance_criteria>
  <done>Three mid-game input components created with constants, following established toggle button and grid patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create GameStatePanel, ReEvaluateButton, and wire into Sidebar</name>
  <read_first>
    - prismlab/frontend/src/components/layout/Sidebar.tsx
    - prismlab/frontend/src/components/draft/GetBuildButton.tsx
    - prismlab/frontend/src/hooks/useRecommendation.ts
    - prismlab/frontend/src/stores/recommendationStore.ts
    - prismlab/frontend/src/components/game/LaneResultSelector.tsx
    - prismlab/frontend/src/components/game/DamageProfileInput.tsx
    - prismlab/frontend/src/components/game/EnemyItemTracker.tsx
  </read_first>
  <files>
    prismlab/frontend/src/components/game/GameStatePanel.tsx,
    prismlab/frontend/src/components/game/ReEvaluateButton.tsx,
    prismlab/frontend/src/components/layout/Sidebar.tsx
  </files>
  <action>
**ReEvaluateButton.tsx -- Same CTA style as GetBuildButton:**
- Import useRecommendation from hooks
- Read recommend and isLoading from the hook
- Render a button with IDENTICAL styling to GetBuildButton:
  - w-full py-3 rounded-lg text-sm font-semibold
  - Active: bg-cyan-accent text-bg-primary cursor-pointer hover:brightness-110 shadow-[0_0_15px_rgba(0,212,255,0.3)]
  - Disabled (isLoading): bg-bg-elevated text-gray-500 cursor-not-allowed animate-pulse opacity-80
- Text: "Re-Evaluate" when idle, "Re-Evaluating..." when loading
- onClick: call recommend() (the same function used by GetBuildButton -- it now sends mid-game fields automatically per Plan 01)

**GameStatePanel.tsx -- Collapsible container for all mid-game inputs:**
- Import LaneResultSelector, DamageProfileInput, EnemyItemTracker, ReEvaluateButton
- Manage local useState boolean for isExpanded, default true
- Render:
  1. A clickable header row: "GAME STATE" label (same h2 style as sidebar section headers) with a chevron icon that rotates when expanded/collapsed. Use a simple inline SVG chevron (path: M6 9L12 15L18 9, strokeWidth 2, stroke currentColor). Apply transform rotate-180 when expanded, transition-transform duration-200.
  2. Collapsible content div with transition-all duration-300 ease-out, max-height animation (max-h-[600px] when expanded, max-h-0 when collapsed, overflow-hidden)
  3. Inside the collapsible:
     - Lane Result section: h3 "Lane Result" label + LaneResultSelector
     - Damage Profile section: h3 "Damage Profile" label + DamageProfileInput
     - Enemy Items section: h3 "Enemy Items Spotted" label + EnemyItemTracker
     - ReEvaluateButton at the bottom with mt-4 spacing
  4. Section label style for h3: text-xs font-medium text-gray-500 uppercase tracking-wider mb-1.5 mt-3
  5. Add a top border: border-t border-bg-elevated pt-4 mt-4

**Sidebar.tsx -- Add GameStatePanel after draft inputs, conditionally:**
- Import GameStatePanel from ../game/GameStatePanel
- Import useRecommendationStore to check if recommendation data exists
- Read hasData from store: const hasData = useRecommendationStore((s) => s.data !== null)
- Inside the scrollable content area (the div with p-4 flex-1 overflow-y-auto), AFTER the Lane Opponents section and BEFORE the closing div:
  Add a conditionally rendered GameStatePanel:
  {hasData && <GameStatePanel />}
- The Game State section appears only after first recommendation is generated (per user decision)
- Keep GetBuildButton pinned at the bottom (unchanged)
  </action>
  <verify>
    <automated>cd prismlab/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - prismlab/frontend/src/components/game/GameStatePanel.tsx exists and exports default component
    - prismlab/frontend/src/components/game/ReEvaluateButton.tsx exists and exports default component
    - grep -n "GameStatePanel" prismlab/frontend/src/components/layout/Sidebar.tsx returns a match
    - grep -n "hasData" prismlab/frontend/src/components/layout/Sidebar.tsx returns a match (conditional render)
    - grep -n "Re-Evaluate" prismlab/frontend/src/components/game/ReEvaluateButton.tsx returns a match
    - grep -n "isExpanded" prismlab/frontend/src/components/game/GameStatePanel.tsx returns a match (collapsible)
    - npx tsc --noEmit exits cleanly with no errors
  </acceptance_criteria>
  <done>Game State collapsible panel with all mid-game inputs and Re-Evaluate button integrated into Sidebar, appearing only after first recommendation</done>
</task>

</tasks>

<verification>
1. Frontend types: cd prismlab/frontend && npx tsc --noEmit
2. All game components exist: ls prismlab/frontend/src/components/game/
3. Sidebar integration: grep -n "GameStatePanel" prismlab/frontend/src/components/layout/Sidebar.tsx
4. Constants added: grep -n "LANE_RESULT_OPTIONS\|DAMAGE_PRESETS\|ENEMY_COUNTER_ITEMS" prismlab/frontend/src/utils/constants.ts
5. Re-Evaluate button: grep -n "Re-Evaluate" prismlab/frontend/src/components/game/ReEvaluateButton.tsx
</verification>

<success_criteria>
- LaneResultSelector renders Won/Even/Lost toggle with correct colors (teal/cyan/red)
- DamageProfileInput has preset buttons and three range sliders
- EnemyItemTracker shows grid of ~15 items with toggle state and item images
- GameStatePanel wraps all inputs in collapsible section with chevron
- ReEvaluateButton triggers recommend() with identical styling to GetBuildButton
- Sidebar shows Game State section only after first recommendation exists
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create .planning/phases/05-mid-game-adaptation/05-02-SUMMARY.md
</output>
