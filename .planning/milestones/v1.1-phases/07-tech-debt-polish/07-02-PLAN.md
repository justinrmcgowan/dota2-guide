---
phase: 07-tech-debt-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prismlab/frontend/src/stores/recommendationStore.test.ts
  - prismlab/backend/tests/test_context_builder.py
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "recommendationStore has tests covering setData, setError, selectItem toggling, togglePurchased, getPurchasedItemIds parsing, clearResults vs clear"
    - "context_builder has tests covering hero lookup, opponent matchup lines, rules formatting, midgame section, and full build assembly"
    - "All new tests pass without modifying production code"
  artifacts:
    - path: "prismlab/frontend/src/stores/recommendationStore.test.ts"
      provides: "Frontend store test coverage"
      min_lines: 80
    - path: "prismlab/backend/tests/test_context_builder.py"
      provides: "Backend context builder test coverage"
      min_lines: 60
  key_links:
    - from: "prismlab/frontend/src/stores/recommendationStore.test.ts"
      to: "prismlab/frontend/src/stores/recommendationStore.ts"
      via: "import useRecommendationStore"
      pattern: "import.*useRecommendationStore.*from.*recommendationStore"
    - from: "prismlab/backend/tests/test_context_builder.py"
      to: "prismlab/backend/engine/context_builder.py"
      via: "import ContextBuilder"
      pattern: "from engine.context_builder import ContextBuilder"
---

<objective>
Fill the test coverage gaps identified during v1.0 by adding tests for the frontend recommendationStore and the backend context_builder.

Purpose: These are the highest-value, lowest-effort test gaps -- recommendationStore is the most complex frontend store, and context_builder is the core logic that will change in Phase 8.
Output: Two new test files with comprehensive coverage of critical paths.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-polish/07-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From prismlab/frontend/src/stores/recommendationStore.ts:
- useRecommendationStore is a Zustand store (create from "zustand")
- State: data (RecommendResponse | null), isLoading (boolean), error (string | null), selectedItemId (string | null), purchasedItems (Set of string)
- Actions: setData(data), setLoading(loading), setError(error), selectItem(phaseItemKey), togglePurchased(phaseItemKey), getPurchasedItemIds(), clearResults(), clear()
- selectItem toggles: if current === key then null, else key
- togglePurchased toggles set membership
- getPurchasedItemIds parses composite keys "phase-itemId" to extract numeric ids, deduplicates
- clearResults keeps purchasedItems, clear resets everything

From prismlab/frontend/src/stores/gameStore.test.ts (test pattern to follow):
- Uses: import { describe, it, expect, beforeEach } from "vitest"
- Uses: store.getState().action() pattern for calling actions
- Uses: store.getState().field for reading state
- Uses: beforeEach with setState to reset store

From prismlab/backend/engine/context_builder.py:
- ContextBuilder class with __init__(opendota_client) and build(request, rules_items, db) async method
- Private methods: _get_hero, _build_opponent_lines, _build_rules_lines, _build_item_catalog, _build_popularity_section, _build_midgame_section
- _build_midgame_section is pure (no DB) -- takes request, returns string
- _build_rules_lines is pure -- takes list of RuleResult, returns string

From prismlab/backend/tests/conftest.py (test pattern to follow):
- Uses: pytest, pytest_asyncio
- test_engine = create_async_engine("sqlite+aiosqlite://")
- test_async_session = async_sessionmaker(test_engine)
- Fixtures: test_db_setup (creates tables + seeds data), test_db_session (yields session)
- Seed data includes heroes (Anti-Mage id=1, Axe id=2, Crystal Maiden id=3, etc.) and items

From prismlab/backend/engine/schemas.py:
- RecommendRequest(hero_id, role, playstyle, side, lane, lane_opponents, allies, lane_result, damage_profile, enemy_items_spotted, purchased_items)
- RuleResult(item_id, item_name, reasoning, phase, priority)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for recommendationStore</name>
  <files>prismlab/frontend/src/stores/recommendationStore.test.ts</files>
  <read_first>
    - prismlab/frontend/src/stores/recommendationStore.ts
    - prismlab/frontend/src/stores/gameStore.test.ts
    - prismlab/frontend/src/types/recommendation.ts
  </read_first>
  <behavior>
    - Test: setData stores response and clears error and loading
    - Test: setError stores error and clears data and loading
    - Test: setLoading sets isLoading flag
    - Test: selectItem sets selectedItemId when null
    - Test: selectItem with same key toggles to null (deselect)
    - Test: selectItem with different key switches selection
    - Test: togglePurchased adds key to purchasedItems set
    - Test: togglePurchased with existing key removes it
    - Test: getPurchasedItemIds extracts numeric IDs from composite keys like "laning-48"
    - Test: getPurchasedItemIds deduplicates (same item in different phases)
    - Test: getPurchasedItemIds returns empty array when no purchases
    - Test: clearResults clears data/error/selectedItemId but keeps purchasedItems
    - Test: clear resets everything including purchasedItems
  </behavior>
  <action>
Create prismlab/frontend/src/stores/recommendationStore.test.ts following the exact pattern from gameStore.test.ts:

1. Import { describe, it, expect, beforeEach } from "vitest"
2. Import { useRecommendationStore } from "./recommendationStore"
3. Import RecommendResponse type from "../types/recommendation"

4. Create a mockResponse factory:
   const mockResponse: RecommendResponse = {
     phases: [{ phase: "laning", items: [{ item_id: 48, item_name: "Power Treads", reasoning: "test", priority: "core", conditions: null, gold_cost: 1400 }], timing: null, gold_budget: null }],
     overall_strategy: "Test strategy",
     fallback: false,
     model: "test-model",
     latency_ms: 100,
   };

5. Add beforeEach that resets the store:
   beforeEach(() => {
     useRecommendationStore.setState({
       data: null,
       isLoading: false,
       error: null,
       selectedItemId: null,
       purchasedItems: new Set(),
     });
   });

6. Write describe blocks for: "setData", "setError", "setLoading", "selectItem", "togglePurchased", "getPurchasedItemIds", "clearResults vs clear" -- implementing all behaviors listed above.

Each test should use useRecommendationStore.getState().actionName() to call actions and useRecommendationStore.getState().field to check state.
  </action>
  <verify>
    <automated>cd "C:/Users/Justin/Documents/Projects/Dota 2 Prismlab/prismlab/frontend" && npx vitest run src/stores/recommendationStore.test.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File prismlab/frontend/src/stores/recommendationStore.test.ts exists
    - grep "describe" prismlab/frontend/src/stores/recommendationStore.test.ts returns at least 5 matches (one per describe block)
    - grep "it(" prismlab/frontend/src/stores/recommendationStore.test.ts returns at least 12 matches
    - grep "useRecommendationStore" prismlab/frontend/src/stores/recommendationStore.test.ts returns matches
    - grep "getPurchasedItemIds" prismlab/frontend/src/stores/recommendationStore.test.ts returns matches
    - grep "clearResults" prismlab/frontend/src/stores/recommendationStore.test.ts returns matches
    - All tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>recommendationStore has 13+ test cases covering all store actions and edge cases. All tests pass without modifying production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for backend context_builder</name>
  <files>prismlab/backend/tests/test_context_builder.py</files>
  <read_first>
    - prismlab/backend/engine/context_builder.py
    - prismlab/backend/tests/conftest.py
    - prismlab/backend/engine/schemas.py
    - prismlab/backend/tests/test_rules.py
  </read_first>
  <behavior>
    - Test: _build_rules_lines formats rule items correctly with item name, phase, reasoning
    - Test: _build_rules_lines returns empty string when no rules
    - Test: _build_midgame_section returns empty string when no midgame fields set
    - Test: _build_midgame_section includes lane result when set
    - Test: _build_midgame_section includes damage profile when set
    - Test: _build_midgame_section includes enemy items spotted formatted with title case
    - Test: full build method returns string containing hero name, role, playstyle, side, lane
    - Test: full build includes opponent section when lane_opponents provided
    - Test: full build includes Available Items section
  </behavior>
  <action>
Create prismlab/backend/tests/test_context_builder.py following the existing test patterns:

1. Imports:
   import pytest
   from unittest.mock import AsyncMock, MagicMock
   from engine.context_builder import ContextBuilder
   from engine.schemas import RecommendRequest, RuleResult

2. Create a fixture for ContextBuilder with a mock opendota client:
   @pytest.fixture
   def builder():
       mock_opendota = MagicMock()
       return ContextBuilder(opendota_client=mock_opendota)

3. Create a helper _make_request function (same pattern as test_rules.py):
   def _make_request(**kwargs) -> RecommendRequest:
       defaults = dict(hero_id=1, role=1, playstyle="farming", side="radiant", lane="safe", lane_opponents=[], allies=[])
       defaults.update(kwargs)
       return RecommendRequest(**defaults)

4. Test _build_rules_lines (pure, no DB needed):
   - Test with sample RuleResult list, assert output contains item_name, phase, reasoning
   - Test with empty list, assert returns empty string

5. Test _build_midgame_section (pure, no DB needed):
   - Request with no midgame fields -> returns ""
   - Request with lane_result="won" -> output contains "Won"
   - Request with damage_profile={"physical": 60, "magical": 30, "pure": 10} -> output contains "Physical 60%"
   - Request with enemy_items_spotted=["black_king_bar"] -> output contains "Black King Bar"

6. Test full build method (needs DB -- use test_db_session fixture from conftest.py):
   Mark with @pytest.mark.asyncio
   Use the test_db_session fixture
   Create builder with mock opendota
   Mock get_or_fetch_matchup and get_hero_item_popularity to return None (avoid external calls)
   Mock get_relevant_items to return a simple item list
   Call builder.build(request, [], db)
   Assert result contains "Anti-Mage" (hero name from seeded DB), "Pos 1", "farming"

For the full build tests, use unittest.mock.patch to mock the matchup_service functions:
   from unittest.mock import patch
   @patch("engine.context_builder.get_or_fetch_matchup", new_callable=AsyncMock, return_value=None)
   @patch("engine.context_builder.get_hero_item_popularity", new_callable=AsyncMock, return_value=None)
   @patch("engine.context_builder.get_relevant_items", new_callable=AsyncMock, return_value=[{"id": 48, "name": "Power Treads", "cost": 1400}])
  </action>
  <verify>
    <automated>cd "C:/Users/Justin/Documents/Projects/Dota 2 Prismlab/prismlab/backend" && python -m pytest tests/test_context_builder.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - File prismlab/backend/tests/test_context_builder.py exists
    - grep "ContextBuilder" prismlab/backend/tests/test_context_builder.py returns matches
    - grep "_build_rules_lines" prismlab/backend/tests/test_context_builder.py returns matches
    - grep "_build_midgame_section" prismlab/backend/tests/test_context_builder.py returns matches
    - grep "async def test_" prismlab/backend/tests/test_context_builder.py returns at least 2 matches (for build tests)
    - grep "def test_" prismlab/backend/tests/test_context_builder.py returns at least 6 total test functions
    - All tests pass (pytest exit code 0)
  </acceptance_criteria>
  <done>context_builder has 9+ test cases covering pure helper methods and the full build method with mocked external dependencies. All tests pass without modifying production code.</done>
</task>

</tasks>

<verification>
- cd prismlab/frontend && npx vitest run src/stores/recommendationStore.test.ts exits 0
- cd prismlab/backend && python -m pytest tests/test_context_builder.py -v exits 0
- Both test files exist and have substantial coverage (80+ lines frontend, 60+ lines backend)
- No production code was modified
</verification>

<success_criteria>
1. recommendationStore.test.ts has 13+ test cases covering all store actions, toggle behaviors, composite key parsing, and clear vs clearResults distinction
2. test_context_builder.py has 9+ test cases covering pure methods and the async build method with mocked dependencies
3. All tests pass on first run without modifying any production code
4. Test files follow existing patterns (Vitest for frontend, pytest for backend)
</success_criteria>

<output>
After completion, create .planning/phases/07-tech-debt-polish/07-02-SUMMARY.md
</output>
